# ANSWERS — журнал возражений и ответов

заведён: 2026-08-16 | завела роль: дизайн | разрешение владельца на создание файла: разовое

Один тред — одно возражение, и заводит его та роль, которая возражает; новое возражение
по уже открытому месту дописывается **в существующий тред**, а не заводится вторым.
Заголовок треда — id вида `D1` (D — дизайн, P — продакт, A — архитектор, C — кодер) и
одна строка сути. Под заголовком шесть полей: **документ** — файл, версия и раздел, к
которым возражение относится; **чей документ** — роль, которая вправе внести правку
(своё возражение к чужому документу вносить нельзя, ждём новой версии этого документа);
**исход** — `блокирует` (без ответа нельзя выбрать решение) / `меняет решение` /
`к сведению`; **статус** — `открыт`, пока владелец не ответил, `внесён` после правки
документа, `закрыт` — ставит только владелец; **автор** и дата. Дальше идёт текст
возражения, строка «что нужно от ответа», затем накапливаются ответы владельца с датами,
и последней строкой треда — отметка о внесении в формате
`→ внесено: <документ> <версия> <раздел>`; статус раздела при этом не меняется, закрывает
его владелец. Прочерк в поле означает «пока пусто», а не «не требуется».

Треды `D1`–`D14` соответствуют возражениям `В1`–`В14` из отчёта роли дизайна от
2026-08-16 (проход 1 — по экранам, проход 2 — по состояниям). `D15`–`D18` заведены
2026-08-28, ревизией дизайна v1.3 по трём претензиям владельца к срезу S6, и стоят в конце
файла — по времени, а не в группе `D`. Там же, следом, `A1` — первый тред роли архитектуры,
заведён 2026-08-28 ревизией по PRD v2.5.

**Оговорка о принадлежности.** Координатор назвал документом продакта шесть пунктов
(D1, D2, D3, D9, D10, D11). По моему чтению ещё четыре — D4, D5, D7, D12 — тоже упираются
в PRD (§7.1, R4, AC 7b, AC 9, AC 12 и `[owner, 18]`), поэтому в поле «чей документ» у них
стоит PRD с пометкой. Расхождение названо, а не разрешено самостоятельно: если продакт
читает их как свои — их шесть плюс четыре, если нет — жду одного предложения.

---

## D1 — Входит ли короткое описание в аккордеон и на страницу даты

- **документ:** `.claude/prd-places-of-interest.md` v2.2, §7.2, §7.3, AC 15
- **чей документ:** продакт
- **исход:** блокирует
- **статус:** открыт
- **автор:** дизайн, 2026-08-16
- **внесено:** → внесено: prd-places-of-interest.md v2.3 §7.1 (таблица состава записи),
  §7.2, §7.3, AC 15, AC 16

§7.2 и §7.3 требуют «показывает свои места», AC 15/16/24 говорят только про состав,
порядок и деление по дням. Про короткое описание не сказано ничего ни в одну сторону.
Срез S1 решил это за PRD: `components/PoiList.tsx` печатает `📍 Имя — описание` в обоих
видах. У `Auswe` это 15 описаний внутри карточки тура.

**Что нужно от ответа.** «Только имена» и «имена с описаниями» — это разные компоненты,
а не разные отступы; без ответа вид ни одного из двух экранов выбрать нельзя.

**Ответы:**

- **2026-08-17, владелец, дословно:** «1 в аккордеон не входит, в страницу даты не
  входит». Читается однозначно: короткое описание показывается **только на странице
  списка**; в аккордеоне программы и на странице даты его нет. Возражение отвечено
  полностью, блокировка снята.
- **Расхождение с кодом, зафиксировано, не правится здесь:** сегодняшний
  `components/PoiList.tsx:71` печатает `📍 Имя — описание` в обоих этих видах, то есть
  срез S1 расходится с решением владельца. Правка кодерская и делается после выхода PRD
  v2.3; из этого треда она не вносится.
- **Что ответ двигает в других тредах** (моё чтение, не слово владельца): D2 — карта
  становится единственным содержимым записи в аккордеоне и на странице даты, вопрос «нужна
  ли она там» становится вопросом «остаётся ли там вообще что-то, кроме имени»; D10 —
  предельный объём падает до 15 имён, но список всё ещё стоит перед кнопкой брони; D11 —
  граница длины описания обслуживает теперь один макет, а не два, и перестаёт быть
  блокирующей; D8 — ветка «пустое описание» в `PoiList` теряет смысл вместе с самим
  описанием, но на странице списка вопрос остаётся.

**статус:** открыт — ответ внесён, закрывает владелец.

---

## D2 — Ссылка на карту показана двумя разными способами

- **документ:** `.claude/prd-places-of-interest.md` v2.2, §7.1, §7.2, §7.3, AC 8
- **чей документ:** продакт
- **исход:** блокирует
- **статус:** открыт
- **автор:** дизайн, 2026-08-16
- **внесено:** → внесено: prd-places-of-interest.md v2.3 §7.1, §7.2, §7.3, §7.4 (строка
  «показать на карте» / "locate on map"), AC 8, AC 15, AC 16

На странице списка (`app/[locale]/places/page.tsx`) имя — заголовок, карта — отдельная
ссылка «На карте ↗». В `PoiList.tsx` имя *само* является внешней ссылкой на Google Maps,
с 📍, без видимой подписи и без маркера внешнего перехода (только `title`, которого на
мобильном нет). Один атрибут одного объекта, два аффорданса, три экрана. Второе: PRD
требует ссылку на карту только на странице списка (AC 8, US2) — в §7.2 и §7.3 её нет
вовсе, то есть в двух видах из трёх она добавлена сверх PRD.

**Что нужно от ответа.** Нужна ли карта в аккордеоне и на странице даты; если нужна —
вид общий со страницей списка.

**Ответы:**

- **2026-08-17, следствие ответа по D1** (моё чтение, не слово владельца): описание из этих
  двух видов ушло, значит запись там — это имя и, возможно, ссылка на карту. Вопрос
  переформулирован и остаётся блокирующим: остаётся ли в аккордеоне и на странице даты
  что-либо, кроме имени, и является ли имя ссылкой.
- **2026-08-17, владелец, дословно:** «2 добавить маркер перехода локализованную строку
  "locate on map"». Читается как решённое по существу: переход на карту помечается
  **подписанной локализованной строкой**, а не одной иконкой и не атрибутом `title`.
  Голый 📍 без подписи, как сейчас в `PoiList.tsx`, ответом отменён.
- ~~**Расхождение с PRD**~~ — **ОТМЕНЕНО ответом 2026-08-17 (2), продакту задачи нет.**
  Было записано: ответ ставит ссылку на карту в аккордеон и на страницу даты, а AC 8
  требует её только на странице списка. Второй ответ владельца показал, что расхождения
  никогда не было: карта живёт только на странице списка, ровно как в AC 8. §7.2/§7.3
  править **не нужно**.
- **Чтения, выдвинутые до второго ответа. Все три им сняты; ниже — их судьба, чтобы ни
  одно не осталось действующим:**
  1. *Имя перестаёт быть ссылкой* — **подтверждено и усилено** (см. ответ ниже).
  2. *Одна строка на все три вида* — **ОТМЕНЕНО. Читал неверно.** Я исходил из того, что
     карта есть во всех трёх видах и вопрос лишь в единообразии подписи. Владелец решил
     иначе: вид с картой **один**. Единообразие обеспечено не общей строкой, а отсутствием
     второго и третьего места, где карту надо было бы показывать.
  3. *RU-сторона не дана* — **отвечено**: «показать на карте».
- **Источник видимой строки.** Существующие ключи `openMap` («На карте» / "On the map") и
  `whatWeSee` («Что увидим») придуманы документом архитектуры §6, в PRD их нет — это ровно
  дефект D6. Строка карты этот путь **прошла до конца**: обе её стороны названы владельцем
  (см. ответ ниже), то есть источник у неё есть. У `whatWeSee` его по-прежнему нет — D6,
  вторая половина.
- **2026-08-17, владелец, дословно:** «D2: ссылка на карту видна только в списке
  достопримечательностей, в карточке программы, в карточке тура только сами POI, без
  ссылки на карту. Имя POI не ссылка. Ru-строка "показать на карте"». Тред снят целиком.
  Что решено:
  1. **Ссылка на карту — только на странице списка.** В аккордеоне программы и на странице
     даты одни имена, ссылки нет вовсе. Возражение «один атрибут показан двумя способами»
     снято не выравниванием двух видов, а тем, что вид остался один.
  2. **Имя POI не ссылка нигде.** На странице списка кликается подписанная строка, в двух
     других видах кликать нечего.
  3. **Строка — пара «показать на карте» / "locate on map"**, одна, и только на странице
     списка.
- **Чтение, требующее подтверждения владельца, — одно, и я его принимаю как рабочее:**
  «карточка программы» = аккордеон на главной (`components/TourCard.tsx`), «карточка тура»
  = страница даты (`app/[locale]/tours/[tourEventId]/page.tsx`), то есть обе точки
  рендеринга, кроме страницы списка. Основание: PRD §7 знает ровно три вида, и два из них
  ответ перечисляет. Единственный конкурент на «карточку тура» — `UpcomingTourCard` в
  расписании — исключён тем, что мест не рендерит вовсе и этой фичей не затрагивается
  (`.claude/architecture-places-of-interest.md` §4).
- **Расхождение с кодом, зафиксировано, здесь не правится:** `components/PoiList.tsx:62`
  делает имя POI внешней ссылкой на карту в обоих этих видах — прямое расхождение с
  решением владельца. Правка кодерская, после выхода PRD v2.3. Ключ и обе локали заводит
  кодер: одна пара, используется только страницей списка; существующий `openMap`
  («На карте» / "On the map") она заменяет.

**исход после ответа:** снят.

**статус:** открыт — ответ внесён, закрывает владелец.

---

## D3 — AC 8 требует «имена программ», а годного поля в данных нет

- **документ:** `.claude/prd-places-of-interest.md` v2.2, AC 8, §7.1
- **чей документ:** продакт
- **исход:** блокирует
- **статус:** открыт
- **автор:** дизайн, 2026-08-16
- **внесено:** → внесено: prd-places-of-interest.md v2.3 §7.1 (таблица состава записи),
  AC 8 — поле названо явно: `TourProgram.title`, не `shortTitle`

`lib/poi.ts` берёт `shortTitle`, а это не название, а фрагмент фразы: RU «тур по центру
Остина», «центр Джорджтауна»; EN "Downtown Georgetown". Следствия: разнорегистровое
перечисление в строке «На каких экскурсиях: …»; у трёх программ `shortTitle` совпадает с
названием POI, и запись «Millett Opera House / Visited on: Millett Opera House» читается
как дефект, хотя §6 называет эти три программы валидными; «центр Джорджтауна» в роли
названия экскурсии — это название места. Альтернатива `title` длинная и при нескольких
программах на место превращает строку в абзац.

**Что нужно от ответа.** Какое поле является «именем программы» на странице списка;
выбор может потребовать третьего поля в данных, то есть меняет не вёрстку.

**Ответы:**

- **2026-08-17, владелец, дословно:** «3 пусть будут полные имена программ». Читаю как
  `TourProgram.title`; иного основания нет — это единственное поле с полным названием, а
  `shortTitle` по данным и есть его короткая форма. Третье поле не заводится.
- **Что снято.** Все три следствия возражения: разнорегистровое перечисление уходит —
  `title` во всех двенадцати записях начинается с заглавной; совпадение имени программы с
  именем POI исчезает — у `Milt` `shortTitle` был «Millett Opera House», а `title` —
  «Историческая прогулка по Конгресс-авеню и тайны Millett Opera House!», у `Amhry` —
  «Экскурсия по музею О'Генри», у `Albjwc` — «Экскурсия в Lady Bird Johnson Wildflower
  Center»; «центр Джорджтауна» в роли названия экскурсии становится «Экскурсия по центру
  Джорджтауна». Блокировка снята.
- **Расхождение с кодом, зафиксировано, здесь не правится:** `lib/poi.ts:79` берёт
  `tour.shortTitle`. AC 8 говорит «имена программ», не называя поле, поэтому PRD ответу не
  противоречит — расходится реализация. Правка кодерская, после выхода PRD v2.3.
- **Цена, которую я сам называл, и куда она легла.** Место, которое посещают несколько
  программ, получает перечень полных названий: у `capitol` это три-четыре строки по
  60–80 символов. Это вопрос вида, и он **решается внутри моей зоны** при проектировании
  (перечень строками вместо перечисления через запятую) — новым возражением не становится.
- **Остаток, который в мою зону не входит** — дописан сюда, а не заведён отдельным тредом,
  потому что место то же: `title` у части программ — предложение с восклицательным знаком
  («…и тайны Millett Opera House!»). В строке «На каких экскурсиях: …» такие названия
  читаются как фразы, а не как имена, и вёрсткой это не лечится. Исход — к сведению;
  трогать `data/tours*.ts` эта фича не вправе (§4, non-goals).

**исход после ответа:** снят по существу; остаток — `к сведению`.

**статус:** открыт — ответ внесён, закрывает владелец.

---

## D4 — Страница списка тупик, и обоснование этого фактически неверно

- **документ:** `.claude/prd-places-of-interest.md` v2.2, §7.1, R4, US1
- **чей документ:** продакт *(по моему чтению; в списке координатора этого пункта нет)*
- **исход:** меняет решение
- **статус:** открыт
- **автор:** дизайн, 2026-08-16
- **внесено:** → внесено: prd-places-of-interest.md v2.3 §7.1 (абзац «The program names are
  links»), §3 US1, §4, §10, AC 25; R4 закрыт, пара R4 + R7 из §11.1 удалена

§7.1 и R4 стоят на «у программы нет постоянной цели для ссылки». Для внешней ссылки — да,
для внутренней — нет: у каждой карточки на главной есть якорь
`id={tour.id + 'tour-card'}` (`components/TourCard.tsx:73`), работает событие
`show-all-tours-and-scroll` и `HashScrollHandler`, то есть `/ru/#Acaptour-card`
существует уже сегодня. Оговорка, из-за которой это возражение, а не предложение: на
главной изначально отрисованы не все карточки (3 или 6 из 12, `ToursSection.tsx:51`), так
что якоря может не быть в DOM в момент перехода.

**Что нужно от ответа.** Как есть, US1 («сравнить программы до заявки») страницей списка
не обслуживается: программы названы и недостижимы.

**Ответы:**

- **2026-08-17, владелец, дословно:** «D4 для каждой достопримечательности в списке
  перечисленны программы, которые включают посещение. По нажатию на ссылку пользователь
  переходит на главную к описанию программы, название передается в ссылке. Если программа
  не загружена -- загружаются все программы, и выполняется переход к программе, название
  которой кликнул пользователь». Названия программ на странице каталога становятся
  ссылками на главную с идентификатором программы в адресе; если карточка не отрисована —
  сначала догружаются все, потом переход. Тупик снят, US1 обслуживается. По своей части
  тред закрыт.
- **Что из этого моя зона** (вид, проектируется отдельно, здесь только названо): как
  выглядит перечень программ, когда каждое название стало ссылкой — по ответу на D3 это
  полные `title`, три-четыре строки по 60–80 символов, то есть перечисление через запятую
  из ссылок нечитаемо и перечень становится списком строк; и **чем внутренний переход
  отличается от внешнего**. На одной записи каталога теперь два разных типа перехода:
  ссылка на карту (чужой домен, новая вкладка, подпись «показать на карте» — D2) и ссылка
  на программу (тот же сайт, та же вкладка, прокрутка к карточке). Посетитель обязан
  видеть разницу до нажатия; одинаково оформить их нельзя.
- **Границы того, на что я проектирую** `[from code, проверено координатором]`: якорь
  существует — `id={tour.id + 'tour-card'}` (`components/TourCard.tsx:73`);
  `HashScrollHandler` прокручивает к `#<id>tour-card` после монтирования, опрашивая DOM до
  4 секунд, **но список не разворачивает** и молча сдаётся, если карточки среди
  отрисованных нет; механизм «показать все и прокрутить» есть, но как оконное событие
  `show-all-tours-and-scroll` с `detail.tourId` (`ToursSection.tsx:59-72`), и шлёт его
  сегодня только `UpcomingTourCard` внутри той же страницы. **Межстраничного пути «пришёл
  по адресу с якорем → развернуть все → прокрутить» сегодня нет.**
- **Граница с кодером, названная явно:** недостающее звено — связать хеш при монтировании
  с этим разворотом. Это работа кодера, возможно с решением архитектора о том, где звено
  живёт (`HashScrollHandler` или `ToursSection`). Я определяю вид ссылки и то, что
  посетитель видит по прибытии; механику перехода — нет.
- **Состояние отказа, к сведению:** без JS ссылка всё равно приводит на главную, в секцию
  программ — ни разворота, ни прокрутки не будет, но тупика тоже: посетитель окажется
  там, где программы перечислены. Деградация мягкая, отдельного вида не требует.
- **AC 20 не задет:** ссылка ведёт на существующий адрес главной с хешем, нового URL не
  появляется, а анкорные адреса в `app/sitemap.ts` не попадают намеренно (context.md
  CONSTRAINTS). «Ровно один новый URL» остаётся верным.

**исход после ответа:** снят.

**Дополнение — тестирование, 2026-08-18 (mode 1 по PRD v2.3), по тому же месту.**
**исход: меняет проверку.** Возражение не к решению владельца, а к тому, что у него нет
приёмки на своей обратной стороне. Переход AC 25 меняет три существующих узла главной:
выбор ветки в `TourCard` (архитектура §4 прямо пишет «ограничение ослаблено»), состояние
`ToursSection`, смысл события `show-all-tours-and-scroll` в `HashScrollHandler`. §9.4
(non-regression) не содержит ни одного пункта про поведение списка программ: шаг «показать
ещё» и число карточек в трёх раскладках (6/9/12, 3/6, 3/5) не назван нигде, а именно этот
компонент уже стоил проекту PR #41 и правила `react-hooks/set-state-in-effect`
(context.md вопрос 20). A4 закрывает только вид тизера при заходе **без якоря**. Значит
регрессия в подсчёте карточек или в каскадных ререндерах не имеет критерия, на который её
можно повесить, и по правилу 3 я обязан буду отчитаться о ней как о наблюдении вне AC.
**Что нужно от ответа:** входит ли «список программ на главной ведёт себя как до фичи
(число карточек и шаг „показать ещё“ в трёх раскладках)» в §9.4 отдельным пунктом. Способ
доказательства в проекте уже был и переиспользуем — временный jest с подменённым
`matchMedia`, прогнанный на обеих реализациях (context.md вопрос 20).

**статус:** открыт — ответ внесён, закрывает владелец; дополнение тестирования ответа не
имеет.

---

## D5 — У страницы нет навигации и переключателя языка

- **документ:** `.claude/prd-places-of-interest.md` v2.2, §7.1, AC 12, `[owner, 18]`;
  `.claude/context.md` v1.0, USERS
- **чей документ:** продакт *(по моему чтению; в списке координатора этого пункта нет)*
- **исход:** меняет решение
- **статус:** открыт
- **автор:** дизайн, 2026-08-16
- **внесено:** → внесено: prd-places-of-interest.md v2.3 §7.1 (абзац «No page furniture
  beyond that» и принятая цена, включая названное расхождение с context.md USERS)

`LanguageSwitcher` смонтирован только в `Hero`, то есть только на главной. Прецедент есть
(`/about`, `/tours/calendar` тоже без него), но эта страница — единственная, чья работа
целиком в приёме поискового трафика, а context.md USERS называет переключатель частью
основного пути для посетителя типа 2 (местный, бронирующий для гостей). Пришедший из
Google на `/en/places/` не может отдать гостям RU-версию. Мелочь оттуда же: футер на этой
странице ссылается сам на себя.

**Что нужно от ответа.** Решение владельца, потому что главная навигация исключена
`[owner, 18]`, а переключатель языка — не навигация.

**Ответы:**

- **2026-08-17, владелец, дословно:** «D5 - переключатель языка нужен только на главной
  странице». Страница списка остаётся как есть: шапки нет, есть строка возврата
  «← Austin City Tours» и футер. Прецедент `/about` и `/tours/calendar` подтверждён
  решением, а не унаследован молча. Вида не меняю, по своей части тред закрыт.
- **Цена, принятая владельцем, а не упущенная** — это была половина возражения, поэтому
  записана рядом с ответом: посетитель, пришедший из Google на `/en/places/`, не может
  отдать гостям RU-версию иначе как правкой адреса или обходом через главную;
  `.claude/context.md` v1.0 USERS называет переключатель частью основного пути для
  посетителя типа 2 (местный, бронирующий для гостей). Принято.
- **Путь внутри сайта существует, он длиннее очевидного** `[from code]`. `LanguageSwitcher`
  смонтирован только в `Hero` (`components/Hero.tsx:52`), то есть только на `/`. Полный
  маршрут: `/en/places/` → строка возврата → `/en/` → переключатель → `/ru/` → ссылка в
  футере → `/ru/places/`. Четыре шага, и переключение возвращает на главную, а не на ту же
  страницу: `router.replace(pathname, { locale })` сохраняет путь
  (`components/LanguageSwitcher.tsx:16`), но нажать его можно только там, где он
  отрисован, — то есть путь всегда `/`.
- **`hreflang` этого не меняет** (моё чтение, помечено как чтение). Проставленные на
  странице `en` / `ru` / `x-default` — машинный путь **для поисковика, не для
  посетителя**: это `<link rel="alternate">` в `<head>`, браузер не даёт по ним никакого
  интерфейса, кликнуть их нельзя. Что они дают реально — работают **до** посадки: RU-язычный
  запрос получает в выдаче `/ru/places/`, а не `/en/places/`. То есть правильная локаль
  выбирается на входе, а не переключается на странице; для посетителя типа 2, которому
  нужна **другая** локаль, чем его собственная, hreflang не работает вовсе. Это уточняет
  цену выше, но не отменяет её и решения не касается.
- **Мелочь из того же возражения остаётся здесь, к сведению:** футер на этой странице
  ссылается сам на себя. Ответ её не касается, отдельным пунктом не выношу — это **моя
  зона**: AC 12 требует достижимости страницы из футера **других** страниц, ссылку на себя
  он не требует и не запрещает, и решается это при проектировании вида без ответа
  владельца.

**исход после ответа:** снят.

**статус:** открыт — ответ внесён, закрывает владелец.

---

## D6 — Два соседних списка, различие которых не объяснено

- **документ:** `.claude/architecture-places-of-interest.md` v1.0, §6 (ключ `whatWeSee`);
  `.claude/prd-places-of-interest.md` v2.2, §7.2, R13
- **чей документ:** архитектор (строка), продакт (принятое дублирование)
- **исход:** меняет решение
- **статус:** открыт
- **автор:** дизайн, 2026-08-16
- **внесено:** → внесено: prd-places-of-interest.md v2.3 §7.2, §7.3, §7.4 (пара «Вы увидите» /
  "You will see", обе стороны — слово владельца), R13; O18 заведён и закрыт внутри v2.3

В карточке и на странице даты вплотную стоят «Вам понравится:» (`highlights`, ✓) и «Что
увидим» (места, 📍). R13 принимает дублирование содержимого, но не решает, чем читатель
отличит один список от другого. Подпись `whatWeSee` придумана документом архитектуры §6,
в PRD её нет — видимая посетителю строка не сорсится.

**Что нужно от ответа.** Подтверждение подписи владельцем либо другая пара подписей;
от этого зависит, различаются ли списки словами или формой.

**Ответы:**

- **2026-08-17, владелец, дословно:** «D6 - пока оставляем оба списка». Слово **«пока»**
  сохранено как сказано: решение **провизорное**, той же природы, что выбор Google Maps в
  `.claude/decisions-places-of-interest.md` §5 («reversible»), — в окончательное я его не
  переписываю. Закрывает **первую половину** треда: дублирование содержимого остаётся
  принятым (PRD R13), оба списка живут рядом в карточке и на странице даты, сводить их или
  прятать один я не проектирую.
- **Вторая половина треда остаётся открытой — про подписи и их источник ответ не говорит
  ничего.** Держу её здесь же, отдельным пунктом, а не новым тредом: место возражения то
  же (те же два блока в тех же двух видах), а мои правила требуют дописывать в
  существующий тред, а не заводить второй по тому же месту. Что именно висит:
  «оставляем оба списка» **не является** подтверждением нынешних подписей; `whatWeSee`
  («Что увидим») придумана `.claude/architecture-places-of-interest.md` §6, `openMap`
  оттуда же, «locate on map» пришла ответом по D2 и без RU-стороны. Из трёх видимых строк
  фичи владельцем названа одна. Пока PRD v2.3 не назовёт их сам, посетителю показываются
  строки, источник которых — чужой документ, а не требование.
- **Что от этого зависит в виде:** решение «оба списка остаются» делает подписи
  единственным, что их различает, — то есть цена нерешённой второй половины выросла
  ответом, а не упала.
- **2026-08-17, владелец, дословно:** «D6 - на русском список достопримечательностей в
  программе - "Вы увидите"». Вторая половина треда закрыта: подпись списка мест в
  аккордеоне программы и на странице даты — **«Вы увидите»**; `whatWeSee` («Что увидим»),
  придуманная `.claude/architecture-places-of-interest.md` §6, заменяется словом владельца,
  и у видимой строки появляется источник.
- **2026-08-17, владелец, дословно:** «O18 "you will see"». EN-сторона названа владельцем;
  пара — «Вы увидите» / "You will see". Предложение дизайна ниже совпало с решением, но
  действует решение, а не предложение: у обеих сторон строки теперь источник `[owner]`.
  Регистр и двоеточие отдельным вопросом владельцу не выносятся — приведение к соседней
  `youLike` это деталь заведения ключа, зона кодера. *(дописано продактом при внесении в
  PRD v2.3; тред остаётся дизайновским, статус не меняю)*
- **EN-сторона не дана — предлагаю, помечено как моё, не как слово владельца.** Прецедент
  первичной локализации в проекте есть и им пользовались. Предложение: **"You will see"**
  — прямой эквивалент, тот же залог и то же время, что в RU, и та же конструкция, что у
  соседней `youLike` («Вам понравится:» / "You will love it:"). Пунктуацию (двоеточие)
  предлагаю привести к соседу или снять у обоих — это вопрос кодера при заведении ключа.
  Владелец поправляет одним словом.
- **Подпись на странице каталога ответом не покрыта и автоматически не переносится.** Ответ
  назван для списка «в программе», то есть для двух видов из трёх. На странице каталога
  подпись списку **не нужна по моему проекту**: страница целиком и есть этот список, её
  `h1` («Что посмотреть в Остине») уже выполняет роль подписи, а вторая надстрочная
  подпись над тем же перечнем была бы повтором. Вопрос владельцу не завожу; решение моё и
  лежит в моей зоне.
- **Проверка обещания, ради которой тред и заводился: пара подписей его не выдерживает.**
  «Вы увидите» и соседняя «Вам понравится» — обе второе лицо, обе про будущее впечатление,
  обе стоят вплотную в одном блоке. Различие **формой становится обязательным, а не
  желательным**: словом эти два списка не разводятся. Это моя работа и ответа не требует —
  записано, чтобы обещание не осталось невыполненным молча.

**исход после ответа:** снят целиком (обе половины).

**статус:** открыт — ответ внесён, закрывает владелец.
- **2026-08-17, следствие ответов по D1 и D2** (моё чтение): тред обострился дважды.
  (а) После D1 список мест в аккордеоне — голый перечень имён прямо под перечнем
  `highlights`: два списка стали похожи сильнее, различает их только подпись и иконка.
  (б) Ответ по D2 добавляет **третью** видимую строку («locate on map»), у которой
  источника в PRD тоже нет, — то есть дефект «строка придумана не тем документом»
  тиражируется, а не закрывается. Из трёх видимых строк фичи (`whatWeSee`, `openMap`,
  новая) владельцем названа одна.

---

## D7 — Состояние «пусто»: страница без единого места

- **документ:** `.claude/prd-places-of-interest.md` v2.2, §7.1, §8.1, AC 7b
- **чей документ:** продакт *(по моему чтению; в списке координатора этого пункта нет)*
- **исход:** к сведению
- **статус:** открыт
- **автор:** дизайн, 2026-08-16
- **внесено:** → внесено: prd-places-of-interest.md v2.3 §5.1 (второй пункт «Two states
  therefore do not exist»), §10 (третий инвариант — зона архитектора)

Состояние сегодня возможно: если таблица связей и файлы текстов пусты одновременно,
`Record<never, …>` сходится, билд зелёный, страница отдаёт заголовок и пустой `<ul>` — при
том что ссылка в футере есть на каждой странице, а запись уже в `app/sitemap.ts`. PRD
такого состояния не описывает; AC 7b запрещает только пометки о полноте.

**Что нужно от ответа.** Достаточно одного предложения «такого не бывает, потому что
страница и первый POI едут одним срезом» — тогда это фиксируется как условие среза, а не
как вид.

**Ответы:**

- **2026-08-17, владелец, дословно:** «D7 - таблица связей никогда не пуста». По правилу 3
  это «этого состояния не бывает, потому что»: вида для пустой страницы каталога я не
  рисую, пустой `<ul>` как состояние не проектируется. По своей части тред закрыт.
- **Чем гарантия держится — решением владельца, а не сборкой.** Записано, чтобы она не
  выглядела прочнее, чем есть: два других инварианта фичи ловит компилятор (непарный
  POI — недостающий ключ `Record`, POI без программы — лишний ключ,
  `.claude/architecture-places-of-interest.md` §2.5), а **пустую таблицу связей он
  пропускает**: при пустом `programPoi` выведенный `PoiId` вырождается в `never`,
  `Record<never, …>` сходится с пустыми файлами текстов, `npm run build` зелёный, а
  страница отдаёт заголовок и пустой список — при том что ссылка в футере есть на каждой
  странице, а запись уже в `app/sitemap.ts`.
- **К сведению для архитектора** (инварианты — его зона): третий инвариант, «таблица
  связей непуста», сегодня ничем в сборке не удерживается. Новым вопросом владельцу это не
  заводится — ответ дан, переспрашивать не о чем; чинить или оставить как решение
  владельца, решает архитектор.

**исход после ответа:** снят.

**статус:** открыт — ответ внесён, закрывает владелец.

---

## D8 — Состояние «один элемент» и плейсхолдер: два вида обрабатывают его по-разному

- **документ:** дизайн (вид), `components/PoiList.tsx` и `app/[locale]/places/page.tsx`
  (реализация среза S1); опирается на `.claude/prd-places-of-interest.md` v2.2 §5.1, O17
- **чей документ:** дизайн — вношу я, когда придёт ответ
- **исход:** блокирует
- **статус:** открыт
- **автор:** дизайн, 2026-08-16
- **внесено:** *(продуктовая половина, тред остаётся за дизайном)* → внесено:
  prd-places-of-interest.md v2.3 §5.1 (первый пункт «Two states therefore do not exist»),
  §7.1 (описание — «always present»)

`data/poi.programs.ts` сейчас содержит ровно `Acap: [{ poi: 'capitol' }]`, а
`data/poi.ru.ts` — `description: 'TBD'`: страница «Что посмотреть в Остине» состоит из
одного пункта «Капитолий Техаса — TBD». §5.1 утверждает, что состояния «место без
описания» не бывает вовсе (очередь релиза), но `PoiList.tsx:71` содержит ветку
`{place.description && …}` для пустого описания, а страница списка печатает `<p>`
безусловно и на пустом описании даст пустой абзац. Одно из двух: состояние есть — и тогда
его вид обязан совпасть во всех местах, либо его нет — и тогда ветка в `PoiList` не
сорсится. Вид страницы из одной записи (заголовок 2.5rem по центру, декоративная черта,
один пункт) PRD не описан, а AC 7b запрещает это как-либо комментировать.

**Что нужно от ответа.** Существует состояние «описание пустое» или нет; вид при одной
записи.

**Ответы:**

- **2026-08-17, следствие ответа по D1** (моё чтение): половина возражения снята —
  расхождение между двумя видами исчезает вместе с описанием в `PoiList`. Остаётся
  страница списка: `<p>` печатается безусловно, и при пустом описании даёт пустой абзац,
  а сегодня в данных стоит `'TBD'`. Остаётся блокирующим для страницы списка.
- **2026-08-17, владелец, дословно:** «D8 считаем, что описание есть всегда». По правилу 3
  это «этого состояния не бывает, потому что»: вида для пустого описания я не рисую, и
  безусловный `<p>` на странице списка становится верным, а не рискованным. Блокировка
  снята.
- **Чем держится «не бывает» — не типом.** Компилятор требует ключ, а не текст: `'TBD'` —
  непустая строка, `Record<PoiId, PoiText>` её принимает, AC 3 выполняется буквально.
  Гарантия лежит в процессе, двумя ступенями: **очередь релиза** (PRD §5.1, O17) — место
  связывается в `data/poi.programs.ts` только когда написаны оба описания, поэтому
  ненаписанное место отсутствует везде; и **проверка перед мержем `dev` → `main`** —
  `grep -c "'TBD'" data/poi.ru.ts data/poi.en.ts` даёт 0 (архитектура §7). Записано здесь,
  потому что «состояния не бывает» опирается на дисциплину и на одну команду, а не на
  сборку: если обе ступени пропустить, пустой или плейсхолдерный абзац на странице списка
  физически возможен, и вида для него теперь нет по решению владельца.
- **Сегодняшние данные этому не удовлетворяют:** `capitol` несёт `description: 'TBD'`
  (`data/poi.ru.ts:7`, `data/poi.en.ts`). На `dev` это законно — S1 доказывает путь
  рендеринга, — но в прод такое состояние не едет.
- **Ветка `{place.description && …}` в `PoiList.tsx:71` — два разных основания, не
  сливать.** По **D1** её не станет вместе с самим описанием: в аккордеоне и на странице
  даты описания нет вовсе, значит исчезает и условие, и его содержимое. По **D8** отдельно
  следует, что охранять было нечего: даже там, где описание показывается — на странице
  списка, — пустым оно не бывает. Первое основание убирает код, второе запрещает
  возвращать такую охрану на странице списка. Правка кодерская, после выхода PRD v2.3.

**исход после ответа:** снят.

**Дополнение — тестирование, 2026-08-18 (mode 1 по PRD v2.3), по тому же месту.**
**исход: меняет проверку.** Ответ владельца снял *вид*, но не сказал, какой **исход** я
ставлю сегодняшнему срезу. Состояние в рабочем дереве: `capitol` связан, описание `'TBD'`
(PRD §5.1, архитектура §7). AC 3 при этом выполнен — компилятор доволен, строка непустая,
и PRD говорит это прямым текстом. А AC 1 требует, чтобы каждое место, которое срез вводит,
было «complete in both locales», и §8.1 расшифровывает: *and actually described*. То есть
на репетиционном срезе, который PRD §8 сам и предписывает («A one-place rehearsal, which
stays on `dev`»), AC 1 нарушен по построению. Прочтений два и они дают противоположные
исходы одному и тому же дереву: либо AC 1 к репетиции не применяется (тогда чем это
записано?), либо применяется и репетиция обязана уезжать в отчёт красной. Четвёртого
исхода («нарушено, но допустимо») у меня нет — правило 3.
**Что нужно от ответа:** с какого среза AC 1 проверяется — с первого мержа в `dev` или с
первого среза, который может быть выпущен. Одно предложение.

**статус:** открыт — ответ внесён, закрывает владелец; дополнение тестирования ответа не
имеет.

---

## D9 — Порядок записей на странице списка не задан

- **документ:** `.claude/prd-places-of-interest.md` v2.2, §7.1, AC 7, AC 8, AC 7b
- **чей документ:** продакт
- **исход:** блокирует
- **статус:** открыт
- **автор:** дизайн, 2026-08-16
- **внесено:** → внесено: prd-places-of-interest.md v2.3 §7.1 (абзац «Order: alphabetical»),
  AC 26, §10 (расхождение с архитектурой §2 названо, не разрешено)

AC 7 («стабильный порядок») — про программу, не про каталог. Архитектура выбрала «порядок
первого появления в таблице связей» (§3): посетитель получает места, сгруппированные по
программам, но без подписей групп — порядок, который он не может объяснить. На 30 записях
(а по плану — все 12 программ) это единственная навигация по странице; AC 7b запрещает
счётчики и пометки о полноте, но про группировку и оглавление молчит.

**Что нужно от ответа.** Алфавит / по программам с подписями / по маршруту. Без этого вид
страницы выбрать нельзя.

**Ответы:**

- **2026-08-17, владелец, дословно:** «В9 - алфавитный порядок хорошо». Читаю как порядок
  записей **на странице каталога** — тред заведён именно про него. Порядка мест **внутри
  программы** ответ не трогает: там действует AC 7 (порядок свободный, но стабильный) и
  §13 п. 6 (порядок обхода — v2). Блокировка снята.
- **Конфликт с документом архитектуры, назван вслух, здесь не разрешается.**
  `.claude/architecture-decisions-places-of-interest.md` §2 рассматривал ровно эти три
  варианта и **отклонил алфавит**, приняв «порядок первого появления в
  `data/poi.programs.ts`». Критерий отклонения назван там же: алфавит даёт разный порядок
  в RU и EN и тянет сравнение строк с учётом локали. Владелец решил иначе — решение его, —
  но §2 теперь расходится с ним, и **это правка архитектора**, не моя и не продакта.
  Вместе с §2 отпадает записанное там побочное свойство: страница больше не читается как
  «центр Остина → мистический → Bremond → выходные», географической группировки не будет.
- **Что расхождение порядка между локалями стоит с точки зрения вида** (моя зона):
  ничего не ломает. Страница — плоский перечень независимых записей, между соседями связи
  нет, ни одна запись не ссылается на «предыдущую» или «ниже», нумерации нет, якорей на
  позицию нет. Одна и та же страница на двух языках перечислит одно и то же в разном
  порядке — и это ровно то, чего посетитель ждёт от алфавитного списка: RU-читатель ищет
  «Капитолий» на К, EN-читатель — "Capitol" на C. Разъезжается порядок, но не содержание;
  сверять две локали построчно некому и незачем. Единственное, что от этого страдает, —
  приёмка глазами «RU и EN показывают одно и то же»: сравнение перестаёт быть построчным
  и становится сравнением множеств.
- **Ни один критерий приёмки на порядок каталога не опирается** — это и было половиной
  возражения. AC 7 и AC 16 говорят о порядке мест внутри программы, а не в каталоге; AC 4
  требует одинакового порядка **идентификаторов в файлах данных** RU и EN, а не на
  странице, и алфавитная отрисовка его не задевает — порядок ключей в файле и порядок
  вывода это разные вещи. AC 8, AC 13 и AC 14 порядка не касаются.
- **Расхождение с кодом:** `lib/poi.ts:75` (`getPoiCatalog`) возвращает каталог в порядке
  первого появления. Правка кодерская, после выхода PRD v2.3 и правки §2 архитектуры.

**исход после ответа:** снят.

**Дополнение — тестирование, 2026-08-18 (mode 1 по PRD v2.3), по тому же месту.**
**исход: к сведению.** AC 26 и A6 предписывают сверку «RU и EN несут один каталог» как
**сравнение множеств**, но ключа сравнения не называют. На отрендеренном выводе (а по
правилу 6 я смотрю его, а не исходник) у записи нет ничего общего между локалями: имена
разные, идентификатор наружу не печатается. Значит либо сверка сводится к сравнению
количества записей, либо это ручное сопоставление по смыслу — а на 30 записях это уже не
приёмка, а работа. Прочтение, которое я применю без ответа, называю заранее: **сверяю
число записей**, потому что совпадение самих множеств уже удержано типами (§2.5
архитектуры: `Record<PoiId, …>` в обоих текстовых файлах), и глазами на странице можно
поймать только ошибку рендеринга — фильтрацию или потерю записи, — которую число и ловит.
**Что нужно от ответа:** ничего не блокирует; если сверка задумывалась как поимённая,
скажите, и она станет ручной процедурой на владельца.

**статус:** открыт — ответ внесён, закрывает владелец; дополнение тестирования ответа не
имеет.

---

## D10 — Предельный объём внутри карточки тура

- **документ:** `.claude/prd-places-of-interest.md` v2.2, §7.2, AC 7
- **чей документ:** продакт
- **исход:** меняет решение
- **статус:** открыт
- **автор:** дизайн, 2026-08-16
- **внесено:** → внесено: prd-places-of-interest.md v2.3 §7.2 (абзац «The full list stands
  between…», включая поправку посылки: длинных программ две — `Auswe` 15, `Brmn` 10)

У `Auswe` 15 мест, у `Brmn` 10; AC 7 требует показать все. Список встаёт между описанием
программы и кнопкой «Забронировать» (`TourCard.tsx:107` перед `book-button`). На мобильном,
где колонка одна и карточка развёрнута тапом, CTA уезжает на несколько экранов вниз.
Сокращать PRD не разрешает, а «остальное — на странице мест» — это ссылка, которой §7.2
не предусматривает.

**Что нужно от ответа.** Полный список ценой CTA / только имена (см. D1) / первые N плюс
ссылка — последнее требует правки AC 7.

**Ответы:**

- **2026-08-17, следствие ответа по D1** (моё чтение): вариант «только имена» выбран
  владельцем, поэтому объём падает с 15 описаний до 15 строк-имён. Возражение не снято —
  список всё ещё стоит между описанием программы и кнопкой брони, — но перестаёт быть
  выбором между тремя вариантами и становится вопросом одной величины: сколько строк
  допустимо перед CTA. Понижаю до «к сведению» до появления `Auswe` в данных; понижение —
  моё чтение, не слово владельца.
- **2026-08-17, владелец, дословно:** «D10 - пусть так, это только одна такая программа».
  Полный список остаётся на месте, между описанием программы и кнопкой брони; сокращения,
  «первых N» и ссылки «остальное на странице мест» не будет. Вид не меняю. По моей части
  тред закрыт.
- **Фактическое расхождение в посылке — записано, с решением не спорит.** «Одна такая
  программа» верно для 15 мест: столько только у `Auswe`. Но `Brmn` — 10 мест, то есть
  длинных программ в наборе **две**, а не одна `[from code, .claude/architecture-…md §9]`.
  Сверх того наборы **восьми из двенадцати** программ в репозиторий ещё не приезжали
  (сегодня в `data/poi.programs.ts` связан один POI, S1). Если среди этих восьми окажутся
  такие же длинные, посылка изменится молча — решение останется, а основание под ним
  сменится. Условие на решение это не накладывает: даже при трёх-четырёх длинных
  программах ответ «пусть так» остаётся выполнимым, меняется только частота случая.
- **Порог, при котором это стоит переспросить** (моё чтение, не слово владельца): длинных
  программ становится больше двух **или** появляется список длиннее `Auswe`. До этого —
  к сведению; тред уже открыт, возвращать его в блокирующие нечем.

**исход после ответа:** снят по существу; посылка — `к сведению`.

**статус:** открыт — ответ внесён, закрывает владелец.

---

## D11 — «Короткое описание» без границы длины

- **документ:** `.claude/prd-places-of-interest.md` v2.2, §5 (таблица полей), `[owner, 3]`
- **чей документ:** продакт
- **исход:** блокирует
- **статус:** открыт
- **автор:** дизайн, 2026-08-16
- **внесено:** → внесено: `prd-places-of-interest.md` v2.5 §5 (таблица полей), §7.5, §9.3 AC 30
  clause c *(дописка 2026-08-28; блокировка снята ещё ответом по D1)*

«Short by design» — не граница и не проверка, а поле обслуживает два несовместимых
макета: абзац на странице списка и хвост одной строки после тире в аккордеоне. 200
символов нормальны в первом и дают стену во втором. Тот же класс — длинные имена:
`📍 Ann W. Richards Congress Avenue Bridge` в одноколоночной карточке переносится и рвёт
строку-ссылку.

**Что нужно от ответа.** Граница в символах или строках. Гейтит D1 и D10: оба вида иначе
проектируются под неизвестный вход.

**Ответы:**

- **2026-08-17, следствие ответа по D1** (моё чтение): описание обслуживает теперь один
  макет — абзац на странице списка, — а не два несовместимых. Блокировка снята: абзац
  терпит любую разумную длину, и вид страницы списка выбирается без числа. Остаётся «к
  сведению»: без границы «короткое описание» — не требование, и длинные **имена**
  (`Ann W. Richards Congress Avenue Bridge`) по-прежнему рвут строку в одноколоночной
  карточке, потому что имя из аккордеона никуда не делось.
- **2026-08-28, дизайн — то же возражение по новому месту, поэтому дописано сюда, а не заведено
  вторым.** С v2.4 поле обслуживает два макета снова (абзац на странице списка и тело панели), и
  у отсутствия границы появились два следствия, которых в 2026-08-16 не было. Первое: длина
  описания задаёт **длину брендовой полосы слева** — панель без границы длины перестаёт быть
  «tiny», ради чего полоса и выбрана вместо фона [DD §14]. Второе: длина описания решает,
  достижимо ли «панель видна целиком» (дизайн §3.7): описание, которое выше окна, доводится
  только верхом. На залитых данных обе беды не наступают — 88–304 знака, самое длинное это
  ≈ 11 строк ≈ 290 px в самой узкой колонке `[from code, data/poi.ru.ts]`, — то есть **сегодня
  граница есть фактически, но не записана**. Исход прежний, «к сведению»: числа хватило бы
  одного, и оно уже видно в данных.
- **2026-08-28, продакт — внесено; исход оставлен прежним, «к сведению».** Границы в знаках
  документ по-прежнему не назначает, но основание отказа сменилось: раньше — «числа не выдумывают
  до появления текстов», теперь тексты есть и полоса **измерена** — 36 описаний, 88–304 знака,
  среднее 198 `[from code, data/poi.ru.ts, 2026-08-28]`. Записано в §5 строкой поля как
  **измерение, а не предел**: описание длиннее нынешнего максимума не нарушает ни одного критерия
  и ничего не гейтит.
  Два названных следствия разведены по владельцам, чтобы ни одно не осталось без адреса.
  **Длина брендовой полосы** — облик, зона дизайна (PRD §10), PRD её не задаёт и не будет.
  **Недостижимость «панель видна целиком»** — поведение, и с v2.5 у него есть приёмка: AC 30
  clause c описывает панель выше окна отдельной веткой (верх встаёт под верхнюю кромку, низ не
  прижимается никогда), то есть состояние перестало быть умолчанием и получило исход, который
  человек может вынести. Третий читатель поля — вес HTML главной — по-прежнему у R18.
  Условие возврата, названное явно: описание, заметно длиннее нынешних 304 знаков, ничего не
  ломает, но растит R18; вопрос владельцу заводится тогда, а не раньше.
  → внесено: `prd-places-of-interest.md` v2.5 §5 (таблица полей, строка «short description»),
  §7.5 (абзац «A panel taller than the window is a shipped state»), §9.3 AC 30 clause c

---

## D12 — Состояние «отказ»: внешняя ссылка на карту

- **документ:** `.claude/prd-places-of-interest.md` v2.2, AC 9, §7.1
- **чей документ:** продакт *(по моему чтению; в списке координатора этого пункта нет)*
- **исход:** меняет решение
- **статус:** открыт
- **автор:** дизайн, 2026-08-16
- **внесено:** —

Единственный класс отказа, доступный посетителю на трёх экранах фичи, — внешняя ссылка на
карту: чужой домен, новая вкладка, на мобильном уход в приложение Google Maps прямо
посреди сценария брони. AC 9 проверяет корректность ссылки руками и автоматизации не
обещает, то есть промах страница не заметит и посетителю не покажет. Маркировка внешнего
перехода в PRD не описана и сейчас сделана двумя способами (это же D2). Других отказов на
этих экранах нет: страница списка статична, `notFound()` есть только у страницы даты и к
фиче отношения не имеет — фиксирую явно, чтобы состояние не осталось без ответа.

**Что нужно от ответа.** Помечать ли внешний переход и одинаково ли во всех видах.

**Ответы:**

- **2026-08-17, следствие ответа по D2** (моё чтение): первая половина отвечена словом
  владельца — переход помечается, и помечается локализованной подписью, а не иконкой.
  Вторая половина («одинаково ли во всех видах») отвечена только моим чтением 2 из D2 и
  ждёт подтверждения там же. Понижаю до «к сведению»: остаётся не вид, а то, что промах
  ссылки страница не заметит и посетителю не покажет (AC 9 — ручная проверка).
- **2026-08-17, второй ответ по D2 — тред сузился до одного вида.** Внешняя ссылка
  осталась **только на странице списка**: из карточки программы и со страницы даты
  посетителя больше ничего не уводит, то есть половина возражения — «уход в приложение
  Google Maps посреди сценария брони» — **снята устройством**, а не оформлением: в видах,
  где идёт сценарий брони, ссылок нет. Вопрос «одинаково ли помечено во всех видах» отпал
  вместе с чтением 2: помечать негде, вид один.
- **Остаток, из-за которого тред не закрываю совсем** — к сведению: на странице списка
  переход остаётся внешним (чужой домен, новая вкладка, на мобильном — приложение), а
  промах ссылки страница не заметит и посетителю не покажет; AC 9 проверяет ссылки руками
  и автоматизации не обещает. Это не вид и решения не требует — записано, чтобы состояние
  «отказ» не осталось без ответа.

**исход после ответа:** `к сведению` *(было `меняет решение`)*; по виду снят.

---

## D13 — Мобильный вид расходится не там, где записано

- **документ:** `.claude/architecture-places-of-interest.md` v1.0, §4 и §8 A4;
  `.claude/prd-places-of-interest.md` v2.2, AC 15, AC 23; `components/ToursSection.tsx`
- **чей документ:** архитектор (точки рендеринга), продакт (AC 15 как проверяемое)
- **исход:** к сведению
- **статус:** открыт
- **автор:** дизайн, 2026-08-16
- **внесено:** *(продуктовая половина — только оговорка о приёмке)* → внесено:
  prd-places-of-interest.md v2.3 AC 15 («How to check it until GitHub issue #53 is fixed»)

Свёрнутая мобильная карточка — тизер, мест в ней нет, и её не трогаем: это верно. Но
`useMediaQuery` (`ToursSection.tsx:41`) отдаёт на сервере `false`, поэтому в отрендеренном
HTML главной карточки развёрнуты, и их три (`layoutCount = 3`, потому что
`isThreeColumn === false`). Следствия: на мобильном первый кадр — три развёрнутые карточки
со списками мест, схлопывающиеся после гидратации в тизеры; AC 15 на отрендеренном HTML
верен только для первых трёх программ, а context.md вопрос 17 требует проверять именно
отрендеренный HTML; для поисковика описания мест дублируются между главной и страницей
списка ровно для этих трёх.

**Что нужно от ответа.** Ничего не блокирует; фиксирую, чтобы приёмка AC 15 не считалась
пройденной по десктопному экрану.

**Ответы:**

- **2026-08-17, владелец, дословно:** «D13 - давай ты это как issue на github запишешь,
  это надо исправлять, но к проекту не имеет прямого отношения». Заведён issue
  **https://github.com/tatiana-act/austin-city-tours/issues/53** — «Home page SSR renders
  the desktop shape: three expanded tour cards in the HTML».
- **Что вынесено.** Сам дефект — `useMediaQuery` (`components/ToursSection.tsx:41`) отдаёт
  на сервере `false`, поэтому в отрендеренном HTML главной карточки развёрнуты и их три —
  **за границами этой фичи**. Он существовал до неё, воспроизводится без единого POI и
  чинится **отдельной веткой, не веткой фичи**. Ни вида, ни решения по нему я не даю. По
  этой части тред закрыт.
- **Что НЕ вынесено и остаётся внутри фичи** — отдельным открытым пунктом, здесь же, а не
  новым тредом: место возражения то же, и оба утверждения выведены из одного факта.
  **Приёмка AC 15 на отрендеренном HTML верна только для первых трёх программ**, пока
  issue #53 не починен. Это свойство приёмки, а не дефект главной: проверяющий, открывший
  исходный HTML `/ru/`, увидит списки мест трёх программ из двенадцати и может прочитать
  это как «AC 15 не выполняется» либо, наоборот, засчитать AC 15 по десктопному экрану,
  где карточки развёрнуты клиентом. context.md вопрос 17 требует проверять именно
  отрендеренный HTML — то есть ловушка стоит ровно на том пути, который проект считает
  правильным.
  - **чей пункт:** продакт (формулировка AC 15) и тестировщик (процедура проверки).
    Дизайн — источник наблюдения, не владелец правки.
  - **исход:** к сведению. Ни один вид от этого не меняется.
  - **как проверять до починки #53** (моё чтение, не слово владельца): AC 15 проверяется
    на программе, попавшей в первые три карточки, плюс тот же список на странице даты
    (AC 16) — там он в HTML всегда, страница даты серверная и без `useMediaQuery`.

**исход после ответа:** дефект вынесен в issue #53, по этой части тред закрыт; остаётся
открытым пункт про приёмку AC 15.

**статус:** открыт — ответ внесён, закрывает владелец.

---

## D14 — Третье двойное представление, в рисках не записанное

- **документ:** `.claude/architecture-places-of-interest.md` v1.0, §10 п. 6;
  `.claude/prd-places-of-interest.md` v2.2, §6 (Place ↔ Meeting point), R5, R13
- **чей документ:** продакт (риск), архитектор (каталог)
- **исход:** к сведению
- **статус:** открыт
- **автор:** дизайн, 2026-08-16
- **внесено:** *(только риск; ответа владельца в треде нет, выбор одного названия остаётся
  вопросом архитектуры §10 п. 6)* → внесено: prd-places-of-interest.md v2.3 R16, §11.1

`landOffice` — это сегодняшний Capitol Visitors Center, он же точка встречи `Haust`. PRD
§6 запрещает любую связь места и точки встречи, поэтому на странице даты `Haust` один
объект появится дважды, в двух блоках одного экрана, под двумя разными названиями. Это не
R5 (там бонус) и не R13 (там `highlights`) — третий случай. Сейчас спит: в данных только
`capitol`.

**Что нужно от ответа.** Ничего не блокирует; название стоит выбрать одно — это же
открытый вопрос архитектуры §10 п. 6.

**Ответы:** —

---

Треды `T1`–`T7` заведены ролью тестирования 2026-08-18 по PRD v2.3 (mode 1, проход до
кода). Буквы `T` в легенде шапки нет — шапку не правлю; кто ведёт формат, пусть добавит
строку «T — тестирование» либо переименует треды.

---

## T1 — AC 14: на каком хосте «listed URL resolves 200»

- **документ:** `.claude/prd-places-of-interest.md` v2.3, AC 14; `.claude/architecture-places-of-interest.md` v1.1, §8 A1
- **чей документ:** продакт
- **исход:** меняет проверку
- **статус:** открыт
- **автор:** тестирование, 2026-08-18
- **внесено:** —

AC 14 требует, чтобы каждый URL из отрендеренного `sitemap.xml` отдавал 200 «in that exact
form, trailing slash included», а §9 обещает, что критерий проверяем **до** мержа, к
которому относится. До мержа в `main` это невозможно буквально: `SITE_URL` — константа
(`lib/site.ts`, context.md вопрос 30), поэтому и на preview, и на локальном прод-билде
sitemap перечисляет **боевые** адреса, а страницы `/ru/places/` на бою ещё нет. Плюс сам
preview закрыт Vercel SSO и без логина отдаёт 302 (context.md вопрос 29), то есть
процедура A1 «curl по preview» из терминала тоже не даёт 200 ни на чём.

**Прочтение, которое я применю без ответа:** беру из отрендеренного `sitemap.xml` путь и
разрешаю его **против того деплоя, который проверяю** (локальный прод-билд), а совпадение
хоста с `SITE_URL` проверяю отдельно и текстом. При буквальном прочтении AC 14 не может
получить исход «выполнено» ни при какой реализации — тогда он становится блокирующим.

**Что нужно от ответа.** Одно предложение: «URL из sitemap проверяется по пути против
проверяемого деплоя» — или «AC 14 проверяется после релиза, на бою».

**Ответы:** —

---

## T2 — AC 17: состояния, которое он описывает, в данных нет

- **документ:** `.claude/prd-places-of-interest.md` v2.3, AC 17
- **чей документ:** продакт
- **исход:** меняет проверку
- **статус:** открыт
- **автор:** тестирование, 2026-08-18
- **внесено:** —

AC 17 требует, чтобы `bonus` рендерился как сегодня и **никогда внутри списка мест —
включая случай, когда бонус называет место, которое есть в каталоге**. Именно этот случай
в репозитории отсутствует: у всех событий `bonus: ''` (context.md GLOSSARY, «механизм в
проде не используется»). Наблюдать нечего: страница даты не покажет ни одного бонуса, и
честный исход по правилам 3–4 — «не проверено», причём навсегда, а не до следующего среза.

Проверить это можно ровно одним способом — временно проставить существующий ключ
(`bonusCap` при связанном `capitol`) на дату `Acap`, посмотреть страницу даты и вернуть
данные. Это правка как инструмент проверки, а не ремонт, и по моим правилам она должна
быть в плане **до** прогона, а не придумана по ходу.

**Что нужно от ответа.** Санкция на временную мутацию расписания в прогоне (дерево
восстанавливается, доказательство — пустой `git status --porcelain`) — либо согласие, что
AC 17 остаётся «не проверено», пока владелец сам не поставит бонус на дату.

**Ответы:** —

---

## T3 — AC 12: где именно вход из описания тура и в скольких программах

- **документ:** `.claude/prd-places-of-interest.md` v2.3, AC 12, §7.1 («Entry points»)
- **чей документ:** продакт
- **исход:** меняет проверку
- **статус:** открыт
- **автор:** тестирование, 2026-08-18
- **внесено:** —

AC 12 требует **обоих** входов и падает, если нет любого. Футер однозначен. «Reachable
from a tour description» — нет: неопределённый артикль допускает «хотя бы из одного
описания», а US2/US4 и R3 читаются как «из описания тура вообще». Разница даёт разные
прогоны: в первом достаточно одной программы, во втором я обхожу все двенадцать, из
которых восемь сегодня без мест (AC 7a велит им выглядеть ровно как сегодня — то есть без
блока, а значит, возможно, и без входа). Третьим прочтением остаётся «внутри блока мест»
против «в карточке программы независимо от мест».

Фактура, из-за которой это не схоластика: в таблице точек рендеринга архитектуры v1.1 §4
входа из описания тура нет вовсе — перечислены страница списка, аккордеон, страница даты и
ссылка в футере. Прогон по буквальному AC 12 даст «не выполнено» половине критерия.

**Что нужно от ответа.** Где стоит вход (внутри блока мест / отдельной строкой в карточке)
и в каких программах он обязателен — во всех или только в тех, у которых места уже есть.

**Ответы:** —

---

## T4 — AC 21 против строки возврата «← Austin City Tours»

- **документ:** `.claude/prd-places-of-interest.md` v2.3, AC 21; `.claude/architecture-places-of-interest.md` v1.1, §6
- **чей документ:** продакт (критерий), архитектор (исключение)
- **исход:** меняет проверку
- **статус:** открыт
- **автор:** тестирование, 2026-08-18
- **внесено:** —

AC 21 сформулирован без исключений: «No visitor-facing string is hardcoded in JSX», и
context.md DEFINITION_OF_DONE называет это твёрдым правилом. Архитектура §6 заводит
исключение с обоснованием и прецедентом: «← Austin City Tours» на странице списка в
next-intl не заводится, потому что это имя сайта, и так же сделано на `/about` и
`/tours/calendar`. Решение архитектора я не оспариваю (правило 11) — но исход критерия от
него зависит: по букве AC 21 это «не выполнено» с точным свидетельством (строка в JSX,
`app/[locale]/places/page.tsx:48`), по архитектуре — норма. Одна и та же строка получает
два противоположных исхода.

**Что нужно от ответа.** Признаёт ли AC 21 исключение для имени сайта (тогда оговорка
нужна в самом критерии, иначе следующий прогон заведёт дефект заново) — или строку надо
локализовать, как это сделано на странице даты (`t('back')`).

**Ответы:**

- **2026-08-25, владелец, дословно:** «ок, подтверждаю завести ключ и положить в обе локали
  одну и ту же строку». Развилка снята в пользу критерия: исключения у фичи нет, к AC 21
  приводится код, а не критерий к коду.
  → внесено: `architecture-places-of-interest.md` §6 — исключение отменено, строка внесена
  в таблицу слотов с источником «владелец, T4»; там же отграничен прецедент (`/about` и
  `/tours/calendar` держат ту же строку литералом, они вне этой фичи) и разведены парность
  ключа и совпадение значений.
  → кодеру, ближайшим срезом: литерал в `app/[locale]/places/page.tsx:48` → `t(...)` и по
  одному значению в `messages/ru.json` и `messages/en.json`. Входит ли «←» в значение ключа,
  решает кодер — это пунктуация ключа (§6).

---

## T5 — три из шести строк §7.4 не проверяет ни один критерий

- **документ:** `.claude/prd-places-of-interest.md` v2.3, §7.4, §2.1, AC 8, AC 13, AC 21
- **чей документ:** продакт
- **исход:** меняет проверку
- **статус:** открыт
- **автор:** тестирование, 2026-08-18
- **внесено:** —

§7.4 называет шесть видимых строк. Три из них критерий держит: заголовок — AC 13, подпись
карты — AC 8, подпись дня — AC 24. На три оставшихся критерия нет ни одного: **подпись
перечня программ** в записи каталога, **`description` метаданных** страницы и — отдельно —
`<title>`, который по §6 архитектуры собирается как «заголовок + ` | Austin City Tours`»,
то есть буквально не равен строке запроса из §2.1. AC 21 требует только, чтобы строки шли
через next-intl, а не чтобы они были **этими** строками.

Почему это не педантизм. §2.1 в v2.3 прямо говорит, что при «≥20 impressions и <3 clicks»
первое, на что надо смотреть, — двухсловный `description`. Значит документ читает
результат метрики через строку, значение которой ни один критерий не сверяет. При этом в
коде сегодня лежат другие значения (`onTours`, `metaDescription`, `day` — придуманные v1.0
архитектуры), а архитектура v1.1 §6 и §10 п. 7 всё ещё числят эти слоты «без источника»,
хотя PRD §7.4 их уже назвал: EN — слово владельца от 2026-08-18, RU — предложение PRD.
В прогоне это даёт вопрос без ответа: расхождение `messages/*.json` с §7.4 — это дефект
(и по какому критерию), или состояние, о котором я пишу «не проверено»?

**Что нужно от ответа.** Достаточно одной строки: считать ли §7.4 требованием, проверяемым
целиком (тогда исход по каждой из шести строк — обычный), и попадают ли `<title>` и
`description` страницы под проверку значений.

**Ответы:** —

---

## T6 — AC 24 предписывает открыть то, чего в расписании нет

- **документ:** `.claude/prd-places-of-interest.md` v2.3, AC 24, R15; `.claude/architecture-places-of-interest.md` v1.1, §7 (S4)
- **чей документ:** продакт
- **исход:** меняет проверку
- **статус:** открыт
- **автор:** тестирование, 2026-08-18
- **внесено:** —

AC 24 требует деления по дням «in both the accordion and the date page, identically» и
заканчивается процедурой: «Checked by opening `Auswe` in both views». Второй вид открыть
нельзя: R15 в этом же документе фиксирует, что даты `Auswe` нет ни в
`data/upcomingTours.ts`, ни в `data/RecentTours.ts`, и риск назван спящим до первой такой
даты. То есть на срезе S4, который дни и вводит, половина критерия не наблюдаема, а AC 16
(«те же места, тот же порядок, то же деление») наследует ту же дыру для этой программы.

Расписание — контент владельца (context.md, прямые коммиты в `main`), поэтому сам я дату не
завожу и в план не ставлю без санкции.

**Что нужно от ответа.** Либо санкция на временную дату `Auswe` в прогоне с восстановлением
дерева, либо согласие, что до появления настоящей даты половина AC 24 и часть AC 16 стоят
как «не проверено».

**Ответы:** —

---

## T7 — AC 7a: «renders exactly as it does today» — от какого «сегодня»

- **документ:** `.claude/prd-places-of-interest.md` v2.3, AC 7a
- **чей документ:** продакт
- **исход:** к сведению
- **статус:** открыт
- **автор:** тестирование, 2026-08-18
- **внесено:** —

Критерий сравнительный, а базы сравнения не называет. «Сегодня» — это `dev` до ветки среза,
предыдущий релиз или предыдущий срез? На первом срезе разница невелика, на пятом — это
разные утверждения, и одно из них («как в прошлом релизе») незаметно превращает AC 7a в
защиту от накопленного дрейфа.

**Прочтение, которое я применю без ответа:** база — `dev` в точке ответвления проверяемой
ветки; свидетельство — отрендеренный HTML аккордеона и страницы даты для программы без
мест, снятый на обеих сборках. Отдельно отмечаю, что у AC 7a с v2.3 появилось санкционированное
исключение — прибытие по ссылке AC 25 меняет ветку рендеринга карточки (архитектура §4,
приёмка A4), — и в тексте самого критерия его нет.

**Что нужно от ответа.** Ничего не блокирует; если база задумана иной, скажите.

**Ответы:** —

---

Тред `C1` заведён ролью кода 2026-08-19 на срезе S1′.

---

## C1 — Порядок элементов в записи каталога: архитектура и дизайн говорят разное

- **документ:** `.claude/architecture-places-of-interest.md` v1.1, §1 и §4 (таблица точек
  рендеринга, колонка «состав записи»); `.claude/design-places-of-interest.md` v1.0, §1 и §2.2
- **чей документ:** архитектор и дизайнер (границу делят между собой)
- **исход:** меняет решение
- **статус:** открыт
- **автор:** код, 2026-08-19
- **внесено:** → внесено: architecture-places-of-interest.md v1.1 §1 (границы: состав,
  порядок элементов записи, раскладка и форма различия переходов — зона дизайна),
  §4 (таблица точек рендеринга больше не задаёт состав и порядок — ссылка на design
  §2.2 и §3.1–§3.3), §3 (порядок `programs` — порядок отдачи функцией, не показа),
  §8 (преамбула: приёмка архитектуры порядок элементов не проверяет)

Расхождение двойное и обе половины видны в коде среза S1′.

1. **Граница.** Архитектура §1 отдаёт кодеру «вёрстку страницы списка» и «порядок элементов
   внутри записи POI». Дизайн §1 объявляет тем же предметом своё: «состав и порядок элементов
   внутри записи каталога», и сам отмечает расхождение (⚠ в §1), не правя чужой документ.
2. **Собственно порядок.** Архитектура §4 перечисляет состав записи так: имя, описание,
   **подписанная ссылка на карту**, полные имена программ ссылками. Дизайн §2.2 задаёт другой
   порядок: имя, описание, **программы**, и карта последней — с правилом «переход вглубь сайта
   стоит выше перехода с сайта» (§2.2, [DD §4]). Колонка §4 названа указателем, а не
   требованием, но читается как порядок и в этой части противоречит дизайну.

**Что в коде.** `app/[locale]/places/page.tsx` собран по дизайну: имя → описание → подпись
«Можно увидеть на экскурсиях:» и программы по строке ссылками → ссылка на карту последней,
с постоянным подчёркиванием, «↗» и `target="_blank"`. Выбран дизайн, потому что состав и
порядок записи он называет своим предметом прямо и с обоснованием; сам конфликт границ я не
разрешаю (правило 1).

**Что нужно от ответа.** Одно предложение от архитектора: снять из §1 «порядок элементов
внутри записи» и «вёрстку страницы списка» в пользу дизайна — либо, наоборот, назвать
порядок §4 обязательным, и тогда правится дизайн. До ответа код стоит по дизайну.

**Ответы:**

- **2026-08-19, владелец, дословно:** «разрешить конфликт в пользу дизайнера». Обе
  половины треда закрыты одним решением: граница — состав, порядок элементов внутри
  записи каталога, раскладка и форма различия переходов принадлежат
  `.claude/design-places-of-interest.md`; порядок «имя → описание → программы → карта»
  из design §2.2 действует, порядок из архитектуры §4 отменён. Код среза S1′ уже
  собран так и правки не требует.
- **Что изменено в документе архитектуры** (вношу я, архитектор): §1 — перечисленное
  выше вынесено из зоны кодера в зону дизайна, кодеру остаётся исполнение (классы,
  токены, имена ключей, пунктуация, техника переноса, реализация контрактов); §4 —
  колонка «состав записи» снята, вместо неё ссылка на design §2.2 и §3.1–§3.3, так что
  второго источника порядка нет; §3 — сказано явно, что порядок `programs` относится к
  тому, в каком порядке функция их **отдаёт**, а не к тому, как они показаны; §8 —
  преамбула, что приёмка архитектуры состав и порядок элементов не проверяет.
- **Проверено при внесении и исправлено попутно** (архитектор): §4 утверждал, что два
  соседних списка в карточке различает подпись, — design §3.1 решил иначе, различает
  форма. Утверждение снято и заменено ссылкой; иначе конфликт остался бы жить во
  втором месте после того, как разрешён в первом.

---

## C2 — Русская коллация ставит латиницу после кириллицы, а не перед

- **документ:** `.claude/prd-places-of-interest.md` v2.3, R17 и §7.1; `.claude/architecture-places-of-interest.md` v1.1, §3 и §10 п. 9
- **чей документ:** продакт (риск R17), архитектор (§3, §10 п. 9)
- **исход:** к сведению
- **статус:** открыт
- **автор:** код, 2026-08-19
- **внесено:** —

Оба документа утверждают одно и то же следствие алфавитного порядка: «латиница в русской
коллации идёт **перед** кириллицей», поэтому русская страница прочитается как латинский
алфавит, а за ним кириллический. R17 записан прямо для того, чтобы это не завели как
дефект.

**В коде получается наоборот.** Реализация ровно по контракту — `new Intl.Collator(locale)`
(§3), — и на этой платформе она даёт: `['Аврора', 'Капитолий', 'Яблоко', 'Norwood Tower',
'Old Bakery & Emporium']`. Проверено прогоном
(`node -e "new Intl.Collator('ru')"`, Node 26.1.0, полная ICU/CLDR): `compare('Norwood',
'Аврора') === 1`, то есть кириллица первая, латиница после. В `en`-коллации порядок
обратный — сначала латиница, потом кириллица, и там документы совпадают с фактом.

Существо R17 верно — русская страница действительно идёт двумя алфавитами, — неверна
только их последовательность. Значение это имеет ровно одно: тестировщик, читающий R17
дословно, заведёт наблюдаемый порядок как дефект сортировки, хотя это корректный вывод
`Intl.Collator('ru')`. Сегодня не наблюдаемо: в данных одно место; станет видно на первом
же срезе с латинским именем в русской локали (§9: `Old Bakery & Emporium`,
`Millett Opera House`, `Norwood Tower`, `Austin History Center`, `North Cottage`,
`Chateau Bellevue`, `Southwestern University`).

Кода это не меняет: контракт — коллатор локали, PRD AC 26 требует алфавитный порядок, и он
такой и есть. Сам порядок сравнения я не выбираю (§1: чем сравниваются строки — граница
архитектора).

**Что нужно от ответа.** Поправить сторону в R17 и в §3/§10 п. 9 — одна фраза в каждом
месте. Ничего не блокирует.

**Ответы:**

- **2026-08-25, координатор.** Факт перепроверен трижды независимо — кодером (Node 26,
  полная ICU), тестировщиком в прогоне mode 2 и продактом перед правкой: `Intl.Collator('ru')`
  ставит кириллицу перед латиницей. Вторая половина выяснилась на залитых владельцем
  данных: в `data/poi.ru.ts` 37 названий и **ни одно** не начинается с латинской буквы —
  русское имя стоит первым, латинская форма уведена в скобки, — поэтому двух алфавитов на
  странице не возникает вовсе. Посылка R17 («треть набора несёт латинское имя и по-русски»)
  была взята из черновой таблицы имён `[architecture §9]`, то есть из материала, а не из
  данных.
  → внесено: `prd-places-of-interest.md` v2.4 — R17 (направление исправлено, статус
  `low — not realised on the current set`, источник заменён на `[from code, data/poi.ru.ts]`,
  названо условие возврата), §7.1 абзац о порядке, преамбула §11; `[D §1]` — строка ревизии.
  Назначение риска сохранено: корректный вывод сортировки не заводится дефектом.
  Архитектура §3 и §10 п. 9 на прежнюю сторону не опирались.

---

## D15 — Русское имя несёт внутри себя второе имя, и правил на этот счёт нет

- **документ:** `.claude/prd-places-of-interest.md` v2.4, §7.2 («Names only»), AC 15, AC 27;
  данные `data/poi.ru.ts`
- **чей документ:** продакт
- **исход:** меняет решение
- **статус:** закрыт владельцем 2026-08-28
- **автор:** дизайн, 2026-08-28
- **внесено:** — (ничего не вносится)

**19 из 36 русских имён** устроены как «Русское имя (English Name)» `[from code,
data/poi.ru.ts]`: «Художественный центр Джорджтауна (Georgetown Art Center)», «Юго-Западный
университет (Southwestern University)». Пока имя не переносилось, это была одна строка и
вопроса не возникало. С ревизии дизайна v1.3 имя переносится по словам (владелец: «I am ok with
breaking POI names at the list»), и разрыв чаще всего приходится **перед скобкой** — она стоит
ровно в середине длинного имени. Тогда строка перечня начинается с «(Georgetown Art Center) ·»
и читается как отдельный элемент списка.

Второе следствие, независимое от переноса: в перечне имя — контрол с пунктирным подчёркиванием,
и подчёркивание накрывает английское имя внутри русского; на странице каталога то же имя —
инертный `h2`, и там скобка читается как пояснение. Одна и та же строка получает два разных
прочтения в двух видах.

**Что нужно от ответа.** Одно предложение: латинская форма в скобках — часть имени или пояснение
при нём. Часть имени — возражение закрывается «к сведению», ничего не меняется. Пояснение — у
перечня и у каталога появляется правило, которого сегодня нет ни в PRD, ни в дизайне, и это
правка PRD §7.2, а не моя.

Ничего не блокирует: срез S6 показывает имя как есть, в обоих чтениях одинаково.

**Ответы:**

- **2026-08-28, владелец, дословно:** «I don't think D15 is a real problem».
- **Что это закрывает.** Возражение ставило выбор из двух: латинская форма в скобках — часть
  имени или пояснение при нём. Ответ владельца — ветка «ничего не меняется», то есть **часть
  имени**. Правила для перечня и каталога не заводятся, PRD §7.2 не правится, `data/poi.ru.ts`
  остаётся как есть, перенос имени по словам работает и внутри скобки.
- **Оговорка — моё чтение, не слово владельца.** Второе следствие возражения от переноса не
  зависело: в перечне пунктир контрола накрывает английское имя внутри русского, а в каталоге
  та же строка — инертный `h2`. Владелец снял тред целиком, значит снял и эту половину. Если
  она всплывёт на приёмке, поднимать заново, а не считать пропущенной.

**исход после ответа:** к сведению, ничего не вносится.

---

## D16 — Цену перечня PRD принял по английским именам, а данные русские

- **документ:** `.claude/prd-places-of-interest.md` v2.4, §7.2 (абзац «The full list stands
  between the program's description and the booking button»)
- **чей документ:** продакт
- **исход:** меняет решение *(не решение владельца, а число, которым он его принял)*
- **статус:** открыт
- **автор:** дизайн, 2026-08-28
- **внесено:** → внесено: `prd-places-of-interest.md` v2.5 §7.2, §5.1, §6, §8.1, §11

PRD §7.2 уже поправил посылку однажды: «the owner's answer reads "only one such program"; the
data says two». Данные, залитые с тех пор, правят её второй раз, и в ту же сторону.

1. **`Auswe` — 14 мест, а не 15** `[from code, data/poi.programs.ts]`, и они разделены по дням
   (9 + 5), поэтому самый длинный **отдельный** перечень — 9 имён; `Brmn` — 10 без деления.
2. **Высота посчитана по английским именам.** Русские длиннее вдвое: среднее имя 32 знака,
   самое длинное 56 `[from code, data/poi.ru.ts]`. В узкой карточке (256 px) день 1 `Auswe`
   даёт ≈ 13 строк одних только имён по-русски против ≈ 7 по-английски. Прежние числа дизайна
   («6–8 строк») были того же происхождения и в ревизии v1.3 пересчитаны — [DD §3].

Решение — «no truncation, no first N» — этим не оспаривается: оно принято по существу, а не по
числу, и на 13 строках держится так же, как на 7. Оспаривается только то, что цена в документе
записана вдвое меньше действительной, и читатель PRD оценит её неверно.

**Что нужно от ответа.** Поправить в §7.2 число мест (14) и сказать, что принятая цена
относится к русской локали, где она примерно вдвое больше английской. Ничего не блокирует, вид
не меняется.

**Ответы:**

- **2026-08-28, продакт — внесено полностью; решение «no truncation» не пересматривается.**
  Возражение принято в обеих частях. §7.2 переписан на измеренных числах: `Auswe` — **14** мест
  с делением 9 + 5, поэтому самый длинный перечень под одной подписью — девять имён, а самый
  длинный сплошной перечень в данных — **`Brmn`, 10** без деления; **шесть** из двенадцати
  наборов ещё не приехали (в прежнем тексте стояло восемь). Отдельным абзацем записано, что
  высота считалась по английским именам, а связывающая локаль — русская: среднее имя 32 знака
  против 22, самое длинное 56 против 42 `[from code]`, что в узкой колонке даёт примерно вдвое
  больше строк (перенос стоит целой строки): день 1 `Auswe` ≈ 13 строк против ≈ 7 —
  **принятая владельцем цена вдвое больше той, что была описана**.
  Решение при этом стоит: «no truncation» отвечало на вопрос по существу, а не по числу, и
  дизайн на исправленных числах его тоже не оспаривает. Чтобы поправка не осталась без
  последствий, названо **условие возврата к владельцу** (моё чтение, не его слово): сплошной
  перечень длиннее десяти или третья длинная программа среди шести ненаписанных наборов.
  Дописано одной строкой и то, чего в возражении не было: открытая панель временно удлиняет
  перечень перед кнопкой брони, и это улучшение 3, а не побочный эффект.
- **Та же ошибка чтения нашлась ещё в шести местах PRD и исправлена в этот же проход** (правило
  связности): §5.1 (состояние данных — было «одно место, `'TBD'`, файлы не в git»), §6 (деление
  `Auswe` названо числами), §8.1 (репетиционный срез; наблюдаемость AC 25; «8 + 6 + 10 + 15 →
  30 распознанных мест» → 8 + 5 + 10 + 14 → 28, и 36 по шести наборам), R2, R16 (перестал быть
  спящим: `land_office` связан с `Haust`, чья точка встречи — то же здание), R17 (37 → 36 имён),
  R18 (амплификатор измерен: 17 мест и ≈ 3 500 знаков RU-описаний в HTML главной).
  → внесено: `prd-places-of-interest.md` v2.5 §7.2 (абзацы о посылке и о локали), §5.1, §6,
  §8.1, §11 R2/R16/R17/R18, §11.1

---

## D17 — Порог 50 % оставляет обрезанную панель стоять; и на доводку нет критерия приёмки

- **документ:** `.claude/design-places-of-interest.md` v1.3 §3.7 (моя половина);
  `.claude/prd-places-of-interest.md` v2.4, §9.3 AC 28 и §7.5 (половина продакта)
- **чей документ:** дизайн (правило) и продакт (критерий)
- **исход:** меняет решение
- **статус:** закрыт владельцем 2026-08-28
- **автор:** дизайн, 2026-08-28
- **внесено:** → внесено: `design-places-of-interest.md` v1.3 §3.7 (правила 1–8), §2.5 (второе
  применение правила видимости цели), §1 (граница), §6 (порога нет);
  `design-decisions-places-of-interest.md` §19 — **дизайновская половина**;
  `prd-places-of-interest.md` v2.5 §7.5 (улучшение 3), §9.3 AC 30 — **половина продакта**

Владелец `[owner, 2026-08-28]`: «we shall scroll to make it fully visible… **if it is possible**,
we shall scroll if less than 50 % of the popup description is visible». Оговорка отдана мне, и я
её употребил так: **порога нет, доводим всегда, смещая страницу на минимум.**

Довод против порога, а не против числа: порог 50 % оставляет полосу состояний, в которых панель
видна на 60–99 % и **не доводится**, то есть человек читает обрывок — ровно то, на что пришла
претензия. Причина, по которой порог был назван, — защита от рывка — снимается другим средством:
смещение пропорционально нехватке, и панель, обрезанную на пять пикселей, доводят пять пикселей
прокрутки. Названное владельцем при этом выполнено строго, а не сужено: всё, что видно меньше
чем наполовину, доводится; доводится и остальное.

**Второе, и это уже не моё.** Доводка — поведение, а не облик, и ни один критерий приёмки её не
проверяет: AC 28 говорит про содержимое панели и про «одну открытую», про видимость не говорит.
Предложенная формулировка, если продакт возьмёт:

> Клик по имени, панель которого видна не целиком, смещает страницу на минимальное расстояние,
> после которого панель видна целиком; если панель выше окна, её верх встаёт под верхнюю кромку.
> При закрытии панели страница не смещается. Проверяется на телефоне, на программе с делением по
> дням, в обеих локалях.

**С AC 29 и проверкой A8 не конфликтует.** Там прибытие на страницу списка по фрагменту адреса —
другая страница, другой механизм (браузерный переход, `scroll-margin-top`), и переписывать AC 29
не нужно. Общее у них только одно, и это записано в дизайне §3.7 одним правилом видимости цели с
двумя применениями: при прибытии прежнего положения страницы нет и цель ставится под верхнюю
кромку, при раскрытии положение есть и страница смещается на минимум.

**Что нужно от ответа.** Подтвердить, что порог был защитой от рывка, а не самостоятельным
требованием, — и решить, заводится ли критерий приёмки. Ничего не блокирует: правило описано,
срез собирается по нему.

**Ответы:**

- **2026-08-28, продакт — критерий заведён: AC 30.** Предложенная дизайном формулировка взята по
  смыслу и разложена на шесть предложений, каждое из которых проваливается отдельно, — потому что
  проверять это будет человек и ему нужен один исход на случай, а не один на всё поведение.
  Что изменено против предложенного текста, и почему:
  1. **«Минимальное смещение» как таковое не проверяемо глазами** — человек не измеряет минимум.
     Заменено наблюдаемым признаком того же самого: панель **встаёт впритык к той кромке, за
     которую вылезала**, а не по центру окна. Панель, оказавшаяся посреди экрана, критерий
     проваливает, хотя видна целиком.
  2. **Добавлено «уже видимая панель страницу не двигает»** — в предложенном тексте это
     подразумевалось, но не было сказано, а это самый частый случай на десктопе.
  3. **Добавлен пункт про клик по второму имени** — правило отрабатывает один раз и по вновь
     открытой панели; привязано к правилу «одна панель на перечень» (AC 28), чтобы два критерия
     не разошлись.
  4. **Правило 1 дизайна (нет паузы, в которой обрезанная панель успевает быть прочитанной)**
     взято отдельным пунктом; сама анимация оставлена дизайну.
  5. **Правило 8 (плавность, `prefers-reduced-motion`) в критерий не взято** — это облик, зона
     дизайна (§10). Критерий к способу движения безразличен.
  6. **Условия проверки взяты как названо дизайном** (телефон, обе локали, программа с делением
     по дням) и дополнены одним: самое длинное русское описание набора — это и есть случай
     «панель выше окна», иначе он на приёмке не встретится.
- **Порог: записан как решение дизайна, не как слово владельца.** В §7.5 сказано прямо — владелец
  назвал 50 %, дизайн порог снял, употребив его же оговорку «if it is possible», решение ему
  передано и возражений не вызвало. Противоречия с его словом нет: «доводим всегда» включает
  «доводим, когда видно меньше половины». Если владелец порог вернёт, он ложится **условием на
  пункт b AC 30** и больше никуда — это в PRD тоже записано, чтобы возврат стоил одну правку.
- **Терминалом не проверяется ничего, и это сказано в самом критерии.** Тестировщик уже относил
  к «требует человека» показ панели и A10.1/A10.2; AC 30 объявлен человеческой проверкой целиком
  в первой же строке, чтобы исход «не проверено» на нём не появлялся.
  → внесено: `prd-places-of-interest.md` v2.5 §7.5 (улучшение 3, пять абзацев), §9.3 AC 30, §9.3
  AC 28 (указатель на AC 30), §3 US3, §7.3, §8.1, §10 (границы: правило — дизайну, механизм —
  архитектору и кодеру), шапка (маркер `[owner, improvement 3]`)
- **2026-08-28, владелец — порог снят, критерий принят.** Дословно: «if just a small part of the
  popup with description is visible — user can miss it and do not realize it appeared. If large
  fraction of the popup is visible — user will scroll by himself if he is interested. That's where
  50 % comes from, it is just kind of a compromise… I want to keep the code small and simple»; на
  разбор вариантов — «I am agree with what you recommend».
- **Мотив порога был прочитан неверно, вывод от этого не изменился.** Дизайн принял 50 % за защиту
  от рывка. На самом деле порог защищал **заметность**: маленький кусок панели посетитель может не
  заметить и не понять, что вообще что-то раскрылось, а большой — заметит и дочитает сам,
  прокрутив. Это другая задача, чем читаемость, которую решал дизайн. Выводы совпали по причине,
  которую ни одна из сторон не называла: при минимальном смещении величина движения пропорциональна
  нехватке, поэтому «не дёргать страницу, когда видно почти всё» выполняется само собой, без порога.
- **Почему не «меньше двух строк».** Владелец рассматривал замену процента на строки и отказался от
  неё. Довод — размер кода: без порога измерения нет вовсе, `scrollIntoView({ block: 'nearest' })`
  не делает ничего, когда цель видна целиком, и смещает ровно на нехватку, когда не видна; любой
  порог добавляет `getBoundingClientRect`, сравнение с окном и ветвление. Отдельно: процент —
  неустойчивая единица, описания идут 88–304 знака (D11), поэтому 50 % это одна строка у короткой
  панели и пять-шесть у длинной; строки были бы лучше процента, но обе хуже отсутствия порога.
- **исход после ответа:** AC 30 остаётся ровно как написан продактом, пункт b не трогается.
  Оговорка §7.5 о возврате порога сохраняется, но к употреблению больше не ожидается.
- **2026-08-28, продакт — внесено, правило не тронуто.** Изменены две вещи, обе о
  принадлежности, а не о поведении. Первая: отсутствие порога записано теперь как решение
  **владельца** `[owner, decision 2026-08-28]`, а не как употреблённая дизайном оговорка, —
  §7.5 и §10 говорили обратное. Вторая: назван его мотив — порог защищал **заметность**, не
  читаемость; довод, которым v2.5 обосновывала снятие порога («порог оставлял бы панель
  обрезанной при 60–99 %»), был построен на чужом мотиве и заменён на верный: доведённая
  целиком панель заметна не меньше обрезанной, а цена снятия — движение страницы там, где
  посетителю хватило бы видимого, и она принята владельцем. Пункт b AC 30 не тронут ни одним
  словом, оговорка о возврате порога сохранена с пометкой, что к употреблению не ожидается.
  → внесено: `prd-places-of-interest.md` v2.6 §7.5 (абзацы о пороге), §9.3 AC 30 (первая
  строка), §10 (граница дизайна), шапка

---

## D18 — Без JS имя размечено как контрол, которым оно не является

- **документ:** `.claude/design-places-of-interest.md` v1.3 §3.2 и §3.5 (моё);
  `.claude/prd-places-of-interest.md` v2.4, §10 («Telling a clickable name from an inert one»),
  AC 8
- **чей документ:** дизайн (правило), архитектура (разметочный инвариант §4.3), кодер
  (исполнение)
- **исход:** меняет решение
- **статус:** открыт
- **автор:** дизайн, 2026-08-28
- **внесено:** *(архитектура)* → внесено: `architecture-places-of-interest.md` v1.2
  §4.3.2, §4.5, §1 (границы), §8 A12–A13; **v1.3** — момент до гидратации (§4.3.2), условность
  снята с §7 S6′ и §8 A13, §4.5 первый пункт под наблюдаемое требование design v1.4 §3.1;
  разборы — `architecture-decisions-places-of-interest.md` §13, §13.1, §13.1.1. *(дизайн)* → внесено: `design-places-of-interest.md` v1.4 §3.2 (условный признак),
  §3.5 (остаток без скриптов), §1 и §3.1 (элемент контрола), §6, §7; разбор —
  `design-decisions-places-of-interest.md` §20

PRD §10 требует, чтобы посетитель отличал кликабельное имя от инертного **до** клика; дизайн
§3.2 делает это пунктирным подчёркиванием и курсором-указателем. Но признак — статический CSS, и
приезжает он с сервера вместе с разметкой (архитектура §4.3: узел панели присутствует в выводе
рендера всегда). Без JS панель не раскрывается — это записано в §3.5 как состояние отказа, — а
пунктир под именем остаётся. Получается признак, обещающий раскрытие, которого не будет: в
единственном состоянии, где различение действительно нужно, оно работает наоборот.

Само по себе состояние редкое, и вид страницы без JS остаётся тем же, что в v2.3. Возражение
не в том, что что-то сломано, а в том, что **требование §10 в этом состоянии показывается
неверно**, и умолчания здесь быть не должно (правило 3).

**Что нужно от ответа.** Одно решение из двух: (а) признак контрола ставится только после
гидратации — тогда это правило дизайна §3.2 и работа кодера, вида при JS оно не меняет;
(б) состояние без JS правки не стоит — тогда я записываю это в §3.5 явно, и умолчание исчезает.
Сам выбрать не могу: (а) — расход на исполнение, (б) — принятое несоответствие требованию, и оба
за пределами моей границы.

Не блокирует: при работающем JS вид верен.

**Ответы:**

- **2026-08-28, архитектура — расход назван, и при одном способе исполнения он почти нулевой;
  развилка из двух исходов при этом распадается.** По пунктам, которые тред просил назвать:
  1. **(а) исполнимо одним правилом CSS.** Признак остаётся в базовых правилах и **снимается**
     под `@media (scripting: none)`. Разметка не двигается ни на атрибут, клиентского состояния
     не заводится, второго рендера нет.
  2. **Направление правила несущее, и это не деталь.** Написать признак **добавлением** под
     `(scripting: enabled)` нельзя: браузер, которому медиа-признак неизвестен, запрос не
     сопоставляет, правило не применяет — и пунктир исчезает у него навсегда. Редкое состояние
     чинилось бы ценой обычного. Снятие в том же браузере даёт сегодняшнее поведение, уже
     принятое.
  3. **Гидратация не расходится — по построению, а не по везению:** CSS в гидратации не
     участвует. Именно поэтому отвергнут любой вариант (а), трогающий узлы или атрибуты
     (`role`, `tabindex`, `aria-expanded`, флаг «смонтировано»): в React 19 несовпадение при
     гидратации роняет корень в клиентский рендер целиком `[from code, react 19.2.7]`, то есть
     лечимый отказ заменялся бы худшим.
  4. **Второго клиентского состояния (а) не тянет.** Флаг «смонтировано» отвергнут отдельно:
     это `react-hooks/set-state-in-effect`, за который проект уже платил PR #41 (context.md
     вопрос 20), плюс второй рендер каждого перечня.
  5. **Инвариант §4.3 не задет.** Он про **наличие** узла панели в выводе рендера; здесь
     меняется видимость чужого признака у имени. Ни узел, ни серверная разметка описаний не
     трогаются.
  6. **Чего (а) не покрывает, и это остаётся дизайну.** Признак не один: роль кнопки,
     `tabindex` и `aria-expanded` приезжают с сервера и остаются — без JS имя по-прежнему
     объявляется кнопкой и получает фокус. CSS этого не снимает, разметкой снимать запрещено
     (п. 3). **Отсюда выбор между (а) и (б) распадается:** (а) исполняется для видимой половины
     признака практически даром, а остаток — меньший, чем исходный, — записывается в §3.5 как
     принятый. Выбирать осталось одно: делать ли пунктир и курсор условными.
- **Заодно, по тому же элементу, поэтому дописано сюда, а не заведено вторым тредом.** §3.1
  требует заменить `<button>`. Элемент назван архитектурой — `<span role="button" tabIndex={0}>`
  — вместе с перечнем того, что при замене обязано держаться (§4.5): один строчный бокс на имя,
  роль и `aria-*` один в один, `tabIndex`, срабатывание по `Enter` и `Space` с гашением
  прокрутки на `Space`, доступное имя без разделителя, видимый `:focus-visible`. Пунктир и рамка
  продолжаются на второй строке по построению — у строчного элемента они рисуются по фрагментам
  строк. Признак из п. 1 вешается на **этот же** элемент.
  ⚠️ **Расхождение с design §1 названо и не разрешается здесь:** там выбор элемента отдан
  кодеру при условии «переносится как обычный текст». Элемент — разметка, а разметочные
  инварианты фичи ведёт архитектура §4.3; условие дизайна не обойдено — оно первый пункт
  перечня. Кодеру остаётся исполнение.
- **Что осталось у кодера по всему треду:** имя класса и место объявлений признака, запись
  медиа-запроса (Tailwind v4), запись обработчика клавиш, оформление рамки фокуса.
- **2026-08-28, дизайн — выбран (а): признак условен. Внесено, `design` v1.4.** Названная цена
  снимает развилку, а не склоняет её: пока цена была неизвестна, выбор стоял между расходом и
  несоответствием требованию §10, и оба лежали за моей границей. Расход оказался почти нулевым —
  сравнивать осталось одно состояние с одним правилом, и правило дешевле. Редкость состояния
  доводом против не была: редкое состояние с неверным видом остаётся состоянием с неверным видом
  (правило 3). Внесено:
  1. **§3.2 — правило вида.** Без скриптов у имени нет ни пунктира, ни курсора-указателя: оно
     показывается тем, чем в этом состоянии является, — текстом. В таблице трактовок это
     отдельная строка, а не сноска. **Проверяемо одним действием:** при выключенных скриптах имя
     в перечне мест по виду не отличается от имени на странице каталога, при включённых —
     отличается пунктиром.
  2. **Направление правила записано у меня тоже, и как поведение, а не как запись:** признак
     снимается, а не добавляется. Взято от вас без пересмотра; названо в дизайне потому, что от
     направления зависит **вид у третьего браузера** — того, которому медиа-признак неизвестен, —
     а вид мой. Механизм и запись остаются вашими и кодера.
  3. **§3.5 — остаток записан явно, как вы и просили.** Роль кнопки, `tabIndex` и `aria-expanded`
     приходят с сервера и остаются: скринридер объявляет имя кнопкой, `Tab` на него встаёт,
     нажатие не делает ничего. Снять это может только разметка, а расхождение с серверной
     запрещено, и цена запрета выше цены остатка. Видимого вида остаток не касается — у зрячего
     посетителя без скриптов имя от инертного не отличается ничем. Умолчания в §3.5 больше нет.
  4. **Найдено при записи и записано там же — момент до гидратации:** скрипты есть, признак уже
     виден, обработчик ещё не подключён. Тем же средством не лечится, разметкой лечиться не имеет
     права. Принято тем же основанием, что и остаток; отдельным возражением не завожу, потому что
     решения оно не меняет.
  5. **§6 — подписи «включите JavaScript» в этом состоянии нет** и не появится: посетитель не
     теряет ничего из того, что видел до улучшения, а строка была бы второй — с ключом, парой
     переводов и местом в раскладке — ради состояния, у которого теперь верный вид.
  6. **Следствие, которого никто не заказывал, названо, чтобы не читалось как отказ:** в печатном
     представлении документа скриптов нет, поэтому при печати признак снимается тоже. Для печати
     это верный вид; отдельного правила под печать не завожу. Разбор всего пункта — [DD §20].
- **2026-08-28, дизайн — расхождение по элементу контрола снято в вашу пользу, `design` v1.4
  §1 и §3.1.** Элемент — разметка, инварианты разметки ведёт архитектура; моё условие не
  потеряно, оно первый пункт вашего перечня §4.5, и я на него ссылаюсь, а не повторяю его своими
  словами. В §1 выбор элемента переехал из «остаётся кодеру» в «не мои», туда же — механизм
  снятия признака; моим осталось наблюдаемое: перенесённое имя есть **один** контрол — пунктир и
  рамка фокуса продолжаются на второй строке, область нажатия обе строки, доступное имя одно и
  разделителя в нём нет. Прежняя запись «чем заменить — граница кодера» отменена и в [DD §17].
- **К сведению, не правка:** §3.5 уточнён по треду `A1` — «панель выше окна» не создаётся
  **данными** набора, но создаётся **увеличенным шрифтом браузера** на тех же данных. Это
  утверждение о виде и потому моё; чем состояние создаётся на приёмке, решает продакт в `A1`,
  и PRD я не трогаю.

- **2026-08-29, архитектура — ответ на п. 4 (момент до гидратации); §4.3.2 против вашей записи
  держится; одна фактическая поправка к п. 6.**
  1. **§3.2 и §4.3.2 не разошлись.** Вы записали направление правила как поведение и оставили
     механизм здесь — это ровно та граница, на которой раздел и написан. Ни одной правки от
     вашей записи он не потребовал.
  2. **Момент до гидратации: архитектурного средства, которое это состояние убирает, нет, и это
     следует из устройства, а не из бюджета.** Отличить «скрипты доступны» от «обработчик
     подключён» может только то, что подключается вместе с обработчиком, то есть сам JS: CSS
     факта гидратации не видит, серверная разметка о нём знать не может. Значит в окне одна из
     двух вещей неизбежно неверна, и выбирается не наличие неверного состояния, а его знак.
  3. **Средство, меняющее знак, существует — и оно дороже остатка. Цена, а не только факт:**
     императивная запись класса на узел после монтирования. Она дешевле, чем кажется, поэтому
     названа, а не отмахнута: расхождения при гидратации не создаёт (первый клиентский рендер
     совпадает с серверным, класс приходит после), второго рендера не требует, состояния React
     не заводит. Платится другим: признак начинает приезжать **позже разметки у каждого
     посетителя и на каждой загрузке**, то есть редкое состояние чинится видимым изменением
     обычного — тем самым обменом, который вы отвергли в направлении правила. Сверх того обычное
     состояние — ваш предмет, а не мой, и менять его механизмом молча нельзя. Принятие верно.
  4. **Окно, вероятно, у́же, чем читается, но гарантией это не становится.** React записывает
     дискретные события, пришедшие в ещё не гидратированное поддерево, и воспроизводит их после
     гидратации: клик в окне тогда открывает панель с задержкой, а не «не делает ничего».
     Утверждать это из чтения кода нельзя — проверяется на preview, как и клиентская навигация
     §4.1. Вида не меняет: пока панель не открылась, посетитель видит то же, что и сейчас.
  5. **Записан третий запрет** (§4.3.2): окно до гидратации не лечится ни предзагрузочным
     скриптом, ни разметочным различием — оба возвращают запрет о расхождении разметки.
  6. ⚠️ **Поправка к вашему п. 6, к сведению, в ваш документ не вношу.** Печать под этот механизм
     **не попадает**: при печати медиа-признак `scripting` не равен `none` — по спецификации это
     `initial-only`, а движки в печати отдают `enabled`. Ни то ни другое правило снятия не
     включает, поэтому **пунктир печатается**. Если снятие в печати нужно вам как вид, это
     отдельное правило под `@media print` — одна строка у кодера и ваше решение; если не нужно,
     достаточно убрать утверждение. Механизма это не касается ни в какую сторону.

- **2026-08-29, дизайн — поправка о печати принята, ошибка была моя; решение принято и внесено:
  `design` v1.4 §3.2, [DD §20].** Утверждение «в печати скриптов нет, поэтому признак снимается и
  там» неверно, и вывод, стоявший на нём, отменён вместе с ним. Записано наблюдаемое: **при
  печати пунктир печатается**.
  **Отдельного правила под печать не прошу, и причина не в цене одной строки.** Печать в проекте
  не обслуживается ничем `[from code: @media print нет ни в app/globals.css, ни в компонентах]`,
  поэтому правило ради одного глифа стало бы **первым** и объявило бы печать поддержанной
  поверхностью, за которой не стоит ничего: страница уходит на бумагу вместе с кнопками брони,
  свёрнутыми карточками и навигацией. Второй довод — в самом состоянии: признак обманывает тем,
  что обещает раскрытие по нажатию, а на бумаге нажатие невозможно физически, и пунктир там
  читается подчёркиванием в тексте. Требование §10 «отличить до клика» к носителю, на котором
  клика нет, не относится. **Возврат назван:** появятся у сайта правила печати — снятие войдёт в
  них тем же одним правилом, отдельного решения не потребуется.
- **2026-08-29, дизайн — п. 4 уточнён вашей формулировкой, решение прежнее** (`design` v1.4
  §3.2). Было «средствами вида не лечится», стало «средства нет **по устройству, а не по цене**»,
  и рядом названо отвергнутое средство вместе с тем, чем оно платит: признак приезжал бы позже
  разметки у каждого посетителя на каждой загрузке. Так основание принятия читается из документа,
  а не из треда. Пункты 1–5 правок в дизайн не потребовали; запись дискретных событий (п. 4
  вашего ответа) вида не меняет и в документ не идёт — она о механизме и проверяется на preview.

**исход после ответа:** снят — обе половины внесены, поправка о печати принята и внесена
2026-08-29. Для архитектуры: расход назван, механизм и три запрета приняты. Для дизайна: выбран
(а), признак условен, остаток без скриптов записан явно, печать названа отдельным решением,
расхождение по элементу снято.

- **2026-08-29, продакт — внесена продуктовая половина: заведён критерий AC 31.** Дизайн выбрал
  (а) и сказал, что формулировка у него готова, но в PRD не понёс — верно, документ мой.
  Наблюдаемое взято как названо: **при выключенных скриптах имя в перечне по виду не отличается
  от имени на странице каталога, при включённых отличается**; форму признака критерий не
  называет — она ваша (PRD §10), и её смена критерий не переписывает.
  Два обстоятельства внесены **внутрь** критерия как принятое, а не как дефект, чтобы
  проверяющий на них ничего не заводил: остаток разметки (роль кнопки, `tabIndex`,
  `aria-expanded` без скриптов остаются — снять могла бы только разметка, а ей запрещено,
  архитектура §4.3.2) и **момент до гидратации**. Второй в критерий **не взят как проверяемое**:
  воспроизведение клика архитектор наблюдает, но не гарантирует, то есть исход плавает между
  «открывается с задержкой» и «не делает ничего», а пункт с двумя исходами не даёт одного
  вердикта. Критерий, проваливающийся по времени загрузки, приучает списывать красное.
  **Печать: расхождение называю, а не разрешаю молча.** Координатор дал прямое указание печать в
  AC не брать, потому что вы ещё не решили; к моменту правки вы решили (2026-08-29, здесь же):
  правила не будет, пунктир печатается, требование «отличить до клика» к носителю без клика не
  относится. Указание исполнено: печатного состояния в критерии нет. Единственное, что от него
  взято, — область действия «судится на экране», иначе проверяющий с распечаткой завёл бы дефект
  на неописанное. Довнесение вашего решения — одна строка внутрь AC 31, отдельного критерия не
  потребует; жду слова владельца или координатора.
  → внесено: `prd-places-of-interest.md` v2.7 §9.3 AC 31 (новый), §7.2, §8.1, §10, §11, шапка;
  спутник `prd-places-of-interest.changes-v2.7.md`

- **2026-08-29, координатор — слово дано: печать довнести.** Указание, по которому печать в AC не
  бралась, было дано до решения дизайна и им исчерпано; держать документ отставшим на ход больше
  нечем. Взято **ровно решение и ничего сверх**: признак печатается, отдельного правила под печать
  не заводится, требование §10 «отличить до клика» к носителю без клика не относится. Ни формы
  признака, ни правила печати продукт не называет — обе остаются дизайну.
- **2026-08-29, продакт — довнесено; критерия не прибавилось, требования не изменились.** AC 31
  получил область действия и её причину: судится на экране; на бумаге признак остаётся, потому что
  снятие привязано к `scripting`, а при печати он не `none`, и это решение дизайна
  (`design` v1.4 §3.2, 2026-08-29), а не побочный эффект. Записано так, чтобы проверяющий с
  распечаткой **ничего не заводил**, — это единственное, ради чего печать вообще попала в
  критерий. §10 получил одну оговорку того же смысла: требование экранное, и печать ему ничего не
  стоит. §11 — строку о том, что v2.8 рисков не открывает: печать в проекте не обслуживается
  ничем `[from code]`, поэтому названная вне области действия она не снимает никакого покрытия.
  Открытых вопросов ревизия не заводит; §12 пуст, как и был.
  → внесено: `prd-places-of-interest.md` v2.8 §9.3 AC 31 (область действия), §10, §11, шапка;
  спутник `prd-places-of-interest.changes-v2.8.md`

**статус:** открыт — обе половины внесены полностью, отставания от треда больше нет; закрывает
владелец.

---

## A1 — Пункт c критерия AC 30 на сегодняшних данных не наступает, а проверять его предписано на них

- **документ:** `.claude/prd-places-of-interest.md` v2.5 §9.3 AC 30 пункт c;
  `.claude/design-places-of-interest.md` v1.3 §3.5 (состояние «панель выше окна»)
- **чей документ:** продакт (процедура проверки), дизайн (утверждение о достижимости)
- **исход:** меняет проверку
- **статус:** открыт
- **автор:** архитектура, 2026-08-28
- **внесено:** —

AC 30 пункт c предписывает проверять «панель выше окна» на самом длинном русском описании
набора — «which is where this case is reached». Дизайн §3.5 по тем же данным утверждает
обратное: 304 знака в самой узкой колонке (232 px) дают ≈ 11 строк ≈ 290 px, то есть заметно
меньше окна телефона, и состояние «на сегодняшних данных не наступает». Оба утверждения — о
наблюдаемости, и они противоположны. Тестировщик, выполнивший процедуру буквально, увидит
пункт a или b и вынесет по пункту c «пройдено», ни разу не создав состояния, которое пункт
описывает.

Механизма это не меняет и ничего не блокирует: ветка под пункт c написана (архитектура §4.4)
потому, что её требует критерий, а не потому, что состояние встречается. Меняется **проверка**.

Что мешает выбрать условие самостоятельно: альбомная ориентация телефона окно укорачивает, но
одновременно расширяет колонку карточки (сетка `minmax(350px, 1fr)`), то есть панель становится
ниже вместе с окном — состояние может не наступить и там. Надёжно его создаёт увеличение шрифта
браузера: колонка в пикселях та же, знаков в строке вдвое меньше, высота панели растёт кратно.

**Что нужно от ответа.** Одно предложение продакта: чем состояние создаётся на приёмке —
крупным шрифтом браузера, альбомной ориентацией или условием «проверяется, когда в наборе
появится описание, дающее панель выше окна». Пока ответа нет, тестировщик обязан отчитываться
по пункту c как о наблюдении вне AC, а не выносить «пройдено».

**Ответы:**

- **2026-08-28, продакт — возражение принято целиком; процедура пункта c заменена, правило и
  механизм не тронуты.** Проверил сам, а не по чужому утверждению: в `data/poi.ru.ts` 36 описаний,
  88–304 знака, самое длинное — `grace_center` (в EN есть длиннее, 315 знаков); в самой узкой
  колонке телефона это ≈ 11 строк при `leading-relaxed` ≈ 290 px — примерно вдвое меньше окна
  телефона. Дизайн §3.5 прав, а пункт c предписывал искать состояние там, где его нет. Пункт,
  который не может провалиться, — не критерий, и держать его в §9.3 в прежнем виде было нельзя.
- **Условие названо: увеличенный шрифт браузера. Проверено по коду, а не принято на веру.**
  `app/globals.css` нигде не задаёт `html { font-size }`, значит корневой размер — браузерный;
  сетка карточек `repeat(auto-fit, minmax(350px, 1fr))` и `.container { padding: 0 20px }` заданы в
  пикселях, а `.tour-content { padding: 2rem }` и отступ панели (`pl-4`) — в rem. Отсюда: колонка
  держит свою ширину в пикселях, **поле под текст при этом сужается**, число строк и высота строки
  растут одновременно, а высота окна не растёт вовсе. Состояние наступает гарантированно и с
  запасом. Существенно, что это **обычная настройка доступности, а не лабораторный трюк**: значит
  состояние отгружаемое, ветка архитектуры §4.4 написана не «на всякий случай», и проверять её
  честно.
- **Альбомная ориентация отвергнута — счётом, а не осторожностью.** На телефоне в альбомной
  ориентации окно ≈ 375 px по высоте, но карточка становится ≈ 630 px по ширине, панель падает
  примерно до пяти строк ≈ 130 px. Состояние не «может не наступить» — оно там не наступает.
  Записано в PRD §7.5 одной строкой, чтобы никто не пришёл к этому варианту второй раз.
- **Третий предложенный вами вариант — «проверяется, когда появится описание, дающее панель выше
  окна» — не взят.** Это отложенная проверка на неизвестный срок: описания на шесть программ ещё
  не приехали, и ни одно не обязано быть длиннее нынешних. Ветка при этом уже в коде и уже едет в
  прод. Проверять её надо на этом срезе, а не когда-нибудь.
- **Пункт c переписан так, чтобы он умел провалиться, и так, чтобы его нельзя было пройти молча.**
  Три изменения: (1) названо условие — крупнейший шаг шрифта браузера; (2) названа цель — самое
  длинное описание **той локали, которую проверяют**: в RU это `grace_center` в `Gcrt`, в EN —
  `bat_bridge`, и он как раз в `Auswe`. То есть с `Auswe` уходит одна русская половина одного
  пункта, и в закрывающей строке критерия это оговорено; (3) введён
  третий исход: **пункт судится только после того, как открытая панель видимо выше окна; если она
  не выше — случай не создан, и отчитываются именно этим, а не «пройдено»**. Оговорено отдельно,
  что это не тот «не проверено», который в первой строке AC 30 отнесён к терминалу: там средство
  проверки, здесь — отсутствие состояния.
- **Ваше возражение вскрыло больше, чем процедуру.** Тем же ложным чтением был написан абзац
  §7.5 «A panel taller than the window is a shipped state»: он утверждал, что «видно целиком»
  недостижимо на сегодняшних текстах. Абзац исправлен — состояние отгружаемое, но создаётся
  настройкой посетителя, а не длиной описания, — иначе исправление одного пункта AC оставило бы
  ту же ошибку в обосновании.
- **Что НЕ изменилось, названо явно:** правило §3.7 дизайна, ветка архитектуры §4.4, пункты
  a, b, d, e, f критерия AC 30 и его первая строка. Порога не появилось. Ни одного вопроса
  владельцу этот тред не породил.
  → внесено: `prd-places-of-interest.md` v2.6 §9.3 AC 30 clause c и закрывающая строка AC 30,
  §7.5 (абзац «A panel taller than the window…»), §8.1 (строка о том, что нужно AC 30 для
  проверяемости), шапка (версия, провенанс)

- **2026-08-29, архитектура — ответ принимаю целиком; в механизме одно следствие, и оно внесено.**
  Возражение было про наблюдаемость, ответ её и чинит: условие названо, проверено по коду и
  отгружаемо. Что это поменяло у меня: пункт c создаётся **настройкой посетителя**, а не длиной
  описания, значит ветка `'start'` — не страховка на будущее, а путь, по которому посетитель
  ходит уже сегодня. Отсюда в §4.4 добавлено то, чем ветка меряется, чтобы под крупным шрифтом
  она не соврала: высота панели — `getBoundingClientRect().height` после синхронного коммита,
  высота видимой области — `documentElement.clientHeight` (та коробка, по которой выравнивает
  сам `scrollIntoView`), и **ни одной константы в условии**: величина в `rem` растёт вместе с
  панелью, величина в `px` не растёт вместе с текстом — обе ветку опрокидывают. Проверка A11
  получила второй заход под крупный шрифт. Правило §3.7, пункты a, b, d, e, f и порог не
  затронуты ничем.
  → внесено: `architecture-places-of-interest.md` v1.3 §4.4 (чем меряется ветка), §8 A11;
  разбор — `architecture-decisions-places-of-interest.md` §14

**исход после ответа:** снят — процедура названа и проверена по коду; пункт c теперь проваливаем.

**статус:** открыт — ответ внесён, закрывает владелец.

---

Треды `T8`–`T18` заведены ролью тестирования 2026-08-29 на прогоне среза S6′ против PRD v2.8
§9.3/§9.4 и архитектуры v1.3 §8. Ни один из них не оспаривает принятого решения: `D18` и `A1`
закрыты по существу, и переоткрывать их я не прошу — оба уже несут по три и больше записей,
поэтому возражения к **формулировкам критериев**, из этих тредов выросших, стоят отдельно.

---

## T8 — AC 30 a описывает состояние объекта, которого до клика не существует

- **документ:** `.claude/prd-places-of-interest.md` v2.8, §9.3 AC 30 пункт a;
  `.claude/design-places-of-interest.md` v1.4 §3.7 п. 1
- **чей документ:** продакт
- **исход:** меняет проверку
- **статус:** открыт
- **автор:** тестирование, 2026-08-29
- **внесено:** —

Пункт a звучит: «**A panel that was already fully visible** does not move the page. Click it and
nothing scrolls». До клика панели нет: она `hidden` и геометрии не имеет — это записано в
дизайне §3.7 п. 1 («до раскрытия панель скрыта и геометрии не имеет — измерять нечего») и
исполнено `flushSync` в коде. Значит «панель, которая уже была видна целиком» описывает
состояние вещи, которой в этот момент не существует, и допускает два прочтения:

1. **видно было имя** — тогда пункт проваливается штатно и часто: имя у нижней кромки окна
   видно целиком, а панель раскрывается под перечнем своей группы, то есть ниже кромки, и
   страница обязана сместиться;
2. **после раскрытия панель целиком в окне** — тогда пункт про то, что `nearest` на
   помещающейся цели даёт ноль, и он верен всегда, кроме поломки средства.

Это два разных критерия. Прогон вынес «выполнено» по прочтению 2 (смещение 0 на панели,
оказавшейся видимой целиком) — но выбор прочтения сделал проверяющий, а не документ.

Пункты b и c от той же болезни свободны: они говорят про панель уже после раскрытия
(«a panel that was cut off ends up whole», «a panel taller than the window arrives top first»).
Лечится одной формулировкой того же вида: «панель, которая после раскрытия оказалась в окне
целиком, страницу не двигает».

**Что нужно от ответа.** Какое из двух прочтений — критерий. Одной строки достаточно.

**Ответы:** —

---

## T9 — AC 30 объявлен «человеческим целиком», а A11 требует измерения в консоли

- **документ:** `.claude/prd-places-of-interest.md` v2.8, §9.3 AC 30, первая строка;
  `.claude/architecture-places-of-interest.md` v1.3, §8 A11
- **чей документ:** продакт (формулировка средства), архитектура (A11)
- **исход:** меняет проверку
- **статус:** открыт
- **автор:** тестирование, 2026-08-29
- **внесено:** —

AC 30 открывается словами: «it is checked by doing it, and **no part of it can be failed from a
terminal** — it is a human check throughout». Между тем весь фактический прогон пунктов a, b, c,
e, f сделан **инструментально**: `getBoundingClientRect`, `scrollY` до и после, `getClientRects`,
подсчёт `aria-expanded="true"`. Архитектура в A11 не просто допускает такое, а **требует** его:
«в консоли `document.querySelector('.tour-card').scrollTop` и `.scrollLeft` равны нулю», и
объясняет зачем — без второй половины отказ «прокрутилась карточка» неотличим от «не
прокрутилось ничего».

Отсюда вопрос, на который у документа нет ответа: **считается ли измерение в консоли
свидетельством по AC 30?** Три исхода, и они разные:

- да — тогда «human check throughout» неверно, и большая часть AC 30 закрывается воспроизводимо,
  а глазами остаётся только читаемость и пункт d;
- нет — тогда все сегодняшние «выполнено» по a, b, c, e, f надо переписать в «не проверено», и
  критерий ждёт человека с телефоном;
- частично — тогда надо назвать, где проходит граница.

Замечу, что «нельзя провалить из терминала» и «нельзя проверить инструментом» — разные
утверждения, а критерий пользуется первым как если бы оно означало второе.

**Что нужно от ответа.** Одно из трёх выше. Прогон применил первое прочтение и это сказал.

**Ответы:** —

---

## T10 — AC 30 d: «читаемую паузу» нечем мерить, а в одной из двух веток её не бывает по устройству

- **документ:** `.claude/prd-places-of-interest.md` v2.8, §9.3 AC 30 пункт d;
  `.claude/design-places-of-interest.md` v1.4 §3.7 пп. 1, 8
- **чей документ:** продакт
- **исход:** меняет проверку
- **статус:** открыт
- **автор:** тестирование, 2026-08-29
- **внесено:** —

Пункт d: «There is no **readable pause** in which the opened panel stands cut off and the page
moves afterwards». Средство не названо, мера не названа. «Читаемая пауза» — это сколько:
кадр, 100 мс, полсекунды? Глаз человека различает здесь плохо, а видеозапись, трассировка или
`performance.mark` критерием не названы и по первой строке AC 30 (см. `T9`) как будто и не
разрешены.

Вторая половина хуже. Пункт d сам оговаривает: «Whether the motion is animated or instant is
design's (§10) and is not judged here». Но при `prefers-reduced-motion: reduce` прокрутка
мгновенная (дизайн §3.7 п. 8, и код так и написан), то есть пауза между раскрытием и движением
**не наблюдаема в принципе** — оба события в одном кадре. Значит в этой ветке пункт d не может
провалиться, а в другой у него нет средства. Критерий, у которого одна ветка не проваливается,
а вторая не измеряется, вердикта не даёт.

Прямое следствие в этом прогоне: браузерный заход шёл именно ветвью reduced-motion (окно Chrome
было перекрыто, `visibilityState: "hidden"`, и плавную прокрутку Chrome в таком окне не
выполняет). Пункт d вынесен как «не проверено» — и остался бы им, даже если бы прогон
повторили в той же ветке.

**Что нужно от ответа.** Чем пункт d наблюдается и в какой ветке движения он судится. Если
ответ «глазами, на плавной прокрутке, на телефоне» — этого достаточно, но это надо сказать.

**Ответы:** —

---

## T11 — AC 31: «оба выглядят одинаково» ложно по построению

- **документ:** `.claude/prd-places-of-interest.md` v2.8, §9.3 AC 31;
  `.claude/design-places-of-interest.md` v1.4 §3.2
- **чей документ:** продакт
- **исход:** меняет проверку
- **статус:** открыт — ответ внесён, закрывает владелец
- **автор:** тестирование, 2026-08-29
- **внесено:** `prd-places-of-interest.md` v2.9 §9.3 AC 31, шапка, §11; спутник
  `prd-places-of-interest.changes-v2.9.md`

Наблюдаемое AC 31 записано так: «with scripts disabled it carries none and **the two look
alike**». Два имени, которые предлагается сравнить, не могут выглядеть одинаково ни при каком
состоянии скриптов, и это не дефект, а отгруженная вёрстка: имя в перечне программы —
строчный текст базового кегля приглушённым цветом (`<span class="poi-name">` внутри
`<p class="text-ink-muted">`), имя записи каталога — заголовок
`<h2 class="text-ink text-2xl font-bold">` `[from code, отданный HTML /ru/places/]`. Они
различаются кеглем, начертанием и цветом всегда.

Дизайн §3.2 формулирует то же требование корректно и узко: «при выключенных скриптах имя в
перечне мест по виду **не отличается от имени на странице каталога — ни подчёркивания, ни смены
курсора**». То есть сравниваются два свойства, а не облик. PRD, пересказывая это, сравнение
расширил до облика — и тем сделал критерий проваливающимся всегда при буквальном чтении и
проходящим всегда при чтении по дизайну.

Прогон применил чтение дизайна и это сказал. Но выбор прочтения снова сделал проверяющий.

**Что нужно от ответа.** Одна правка формулировки: «признака нет ни у того, ни у другого —
ни пунктира, ни курсора-указателя» вместо «the two look alike». Ни требования, ни решения это
не меняет.

**Ответы:**

- **2026-08-29, продакт — возражение принято целиком, формулировка переписана.** Проверил сам:
  имя записи каталога — `<h2 class="text-ink text-2xl font-bold">`
  `[from code, app/[locale]/places/page.tsx:72]`, имя в перечне — `<span class="poi-name">`
  внутри `<p class="text-ink-muted">`. Облик различается всегда, значит «the two look alike»
  ложно при любом состоянии скриптов, и критерий, у которого исход зависит от того, какое из
  двух прочтений выберет проверяющий, вердикта не даёт. Сравнение сужено до **признака**:
  «с выключенными скриптами его не несёт ни одно из двух имён». Отдельно и явно записано, что
  различие кегля, начертания и цвета — **не предмет** этого критерия: без такой строки первый же
  проверяющий завёл бы дефект на вёрстку, то есть я заменил бы одну ловушку другой.
  Источник формулировки — дизайн v1.4 §3.2, он был узок с самого начала; расширил его мой
  пересказ, и правка возвращает пересказ к источнику. Ни требование §10, ни решение дизайна,
  ни область действия из v2.8 не тронуты.
  → внесено: `prd-places-of-interest.md` v2.9 §9.3 AC 31 (наблюдаемое), шапка, §11;
  спутник `prd-places-of-interest.changes-v2.9.md`

---

## T12 — AC 31: браузер, не знающий `scripting`, принят механизмом, но не назван в критерии

- **документ:** `.claude/prd-places-of-interest.md` v2.8, §9.3 AC 31;
  `.claude/architecture-places-of-interest.md` v1.3 §4.3.2;
  `.claude/design-places-of-interest.md` v1.4 §3.2
- **чей документ:** продакт
- **исход:** меняет проверку
- **статус:** открыт — ответ внесён, закрывает владелец
- **автор:** тестирование, 2026-08-29
- **внесено:** `prd-places-of-interest.md` v2.9 §9.3 AC 31 (третий принятый пункт, браузер
  суждения); спутник `prd-places-of-interest.changes-v2.9.md`

Правило намеренно записано снятием: браузер, которому медиа-признак `scripting` неизвестен,
запрос не сопоставляет, **признак у него остаётся всегда** — и без скриптов тоже. Это несущее
решение, названное и в архитектуре §4.3.2, и в дизайне §3.2, и обоснованное там же: обратная
запись чинила бы редкое состояние ценой обычного.

AC 31 этого состояния не называет. Он перечисляет **два** принятых обстоятельства — остаток
разметки и момент до гидратации — и требует: «with scripts disabled it carries none». Значит
проверяющий, выключивший скрипты в браузере без поддержки `scripting`, обязан по букве завести
дефект на решение, которое принято. Список «принятого, по которому ничего не заводят» тем самым
неполон ровно на то состояние, ради которого правило и написано наоборот.

Практическая сторона: сегодня признак известен Chrome ≥ 120 и Safari ≥ 17. Критерий не говорит,
на чём его проверять, и «switching scripts off and back» в разных браузерах даёт разные
результаты, оба ожидаемые.

**Что нужно от ответа.** Две строки: (1) третье принятое обстоятельство — в браузере без
поддержки `scripting` признак остаётся, и это не дефект; (2) на чём критерий судится — назвать,
что нужен браузер, знающий признак.

**Ответы:**

- **2026-08-29, продакт — принято, заведено третьим пунктом принятого.** Возражение точное:
  правило написано снятием именно ради этого браузера, а критерий требовал от него того, чего
  решение ему не обещает, — то есть обязывал завести дефект на принятое. Третий пункт в списке
  «принятого, по которому ничего не заводят» назван вместе с основанием (архитектура §4.3.2,
  дизайн v1.4 §3.2), чтобы читатель видел не исключение, а решение.
  Вторую половину — на чём судить — взял тоже: названы версии, с которых признак известен
  (Chrome 120+, Safari 17+), и сказано, что на прочих браузерах действует третий пункт. Списка
  поддерживаемых браузеров у проекта нет, поэтому это версии появления признака, а не политика
  поддержки; появится список — строка заменится ссылкой на него.
  → внесено: `prd-places-of-interest.md` v2.9 §9.3 AC 31 (третий принятый пункт и браузер, в
  котором судится); спутник `prd-places-of-interest.changes-v2.9.md`

---

## T13 — AC 28 требует текст «для каждого места из списка», не имея оговорки AC 15 про issue #53

- **документ:** `.claude/prd-places-of-interest.md` v2.8, §9.3 AC 28 (первая половина) и AC 15
  (абзац «How to check it until GitHub issue #53 is fixed»);
  `.claude/architecture-places-of-interest.md` v1.3 §4.3.1
- **чей документ:** продакт
- **исход:** меняет проверку
- **статус:** открыт
- **автор:** тестирование, 2026-08-29
- **внесено:** —

AC 28 говорит: «the panel's **text is present in the rendered HTML for every listed place**, in
the locale served — checkable from a terminal». На главной это неверно и не может быть верным:
из-за issue #53 серверно отрисованы только первые три карточки, поэтому в HTML `/ru/` лежат
описания мест `Acap`, `Haust` и `Gcrt` — 20 узлов панели на 17 мест — и ни одного места
остальных программ. Это не дефект: архитектура §4.3.1 считает границу дублирования ровно по
этому и называет число, а AC 15 несёт целый абзац с процедурой обхода («check this criterion on
a program among those three and take the rest from AC 16»).

У AC 28 такого абзаца нет, а именно он проверяется грепом. Проверяющий, выполнивший AC 28
буквально на главной, получит отсутствие текста для девяти программ из двенадцати и обязан
будет вынести «не выполнено» — по состоянию, которое соседний критерий объявил ожидаемым.

**Что нужно от ответа.** Одна строка внутрь AC 28: до починки #53 первая половина проверяется
на серверно отрисованных карточках и на странице даты, где список в HTML всегда. Прогон именно
так и сделал, и это сказал.

**Ответы:** —

---

## T14 — A11 берёт первую карточку страницы, а не проверяемую, и потому не может провалиться

- **документ:** `.claude/architecture-places-of-interest.md` v1.3, §8 A11
- **чей документ:** архитектура
- **исход:** меняет проверку
- **статус:** открыт — ответ внесён, закрывает владелец
- **автор:** тестирование, 2026-08-29
- **внесено:** `architecture-places-of-interest.md` v1.4 §8 A11, шапка

Процедура A11 предписывает открыть панель «у места в нижней части развёрнутой карточки на
главной» и убедиться, что содержимое карточки не сдвинулось, сняв
`document.querySelector('.tour-card').scrollTop` и `.scrollLeft`.

`document.querySelector` возвращает **первое** совпадение в документе. На главной карточек на
экране три и больше, и «карточка в нижней части экрана», у которой открывают панель, первой не
является почти никогда. То есть измеряется не тот элемент, в котором шёл клик, а соседний, у
которого прокрутки заведомо нет. Проверка даёт нули при любом исходе — в том числе при том
самом отказе, ради которого она написана («прокрутилась карточка, а не страница»). Проверка,
которая не умеет провалиться, отказ не ловит.

Лечится адресацией: снимать надо у предка, содержащего нажатое имя, — `event.target.closest('.tour-card')`
или заранее сохранённая ссылка на узел карточки. Второй заход A11 (крупнейший шаг шрифта) той
же правки требует.

Замечу отдельно, что нулевые значения ожидаются и по выводу самой §4.4 (диапазон прокрутки у
`.tour-card` нулевой), поэтому «получили нули» само по себе ничего не подтверждает — важно,
чтобы нули были сняты с правильного узла, иначе будущее появление диапазона проверка проспит.

**Что нужно от ответа.** Правка одной строки процедуры. Механизма и решения это не касается.

**Ответы:**

- **2026-08-29, архитектура — принято, процедура исправлена.** Возражение верное и по адресу:
  `document.querySelector('.tour-card')` возвращает первую карточку документа, а нажатое имя
  почти никогда не в ней, — значит нули снимались с соседа, у которого прокрутки нет ни при
  каком исходе. Половина проверки, написанная ровно против отказа «прокрутилась карточка, а не
  страница», этот отказ пропускала.
  Процедура теперь адресует карточку, **в которой шёл клик**: `$0.closest('.tour-card')` при
  выделенном имени. Взял и вторую часть замечания — что нули сами по себе ничего не
  подтверждают, поскольку диапазон прокрутки у `.tour-card` сегодня нулевой по выводу §4.4:
  это записано в самой процедуре, иначе будущее появление диапазона она проспит. Второй заход
  (крупнейший шаг шрифта) правится тем же. Механизм, запреты и срез не тронуты.
  → внесено: `architecture-places-of-interest.md` v1.4 §8 A11, шапка

---

## T15 — A9: «текст обязан читаться» с выключённым JS против описаний в скрытых узлах

- **документ:** `.claude/architecture-places-of-interest.md` v1.3, §8 A9;
  `.claude/design-places-of-interest.md` v1.4 §3.5 (состояние «отказ»)
- **чей документ:** архитектура
- **исход:** меняет проверку
- **статус:** открыт
- **автор:** тестирование, 2026-08-29
- **внесено:** —

A9 заканчивается так: «Проверять с выключенным JS тоже: **текст обязан читаться**, блок при
этом может не открываться — это принято».

Прочитанное как есть, это невыполнимо и притом навсегда. Узлы панелей приезжают с атрибутом
`hidden`, а `hidden` в отданном CSS даёт `display:none!important`; открыть их без JS нечем,
потому что открытие — смена состояния React. Значит на экране без скриптов описание не читается
ни при каком действии; читается оно **в документе**. Дизайн §3.5 говорит именно это и разводит
понятия: «Читаемость описаний без JS — свойство механизма (в разметке они есть или нет), а не
вида».

Разница не словесная: проверяющий, применивший A9 буквально, откроет страницу без скриптов, не
увидит ни одного описания и заведёт дефект на принятое поведение. Проверяющий, применивший
чтение дизайна, посмотрит в исходник документа — а туда он уже посмотрел первой половиной той же
A9, и второй заход ничего не добавляет.

**Что нужно от ответа.** Заменить «текст обязан читаться» на «текст обязан **присутствовать в
документе**; на экране он без скриптов не появляется, и это принято». Тогда у второй половины
A9 появляется собственное содержание: она проверяет, что выключение скриптов не убирает узлы.

**Ответы:** —

---

## T16 — у среза, который везёт в себе PR #56, нет базы non-regression

- **документ:** `.claude/prd-places-of-interest.md` v2.8, §9.4 (AC 18–24);
  `.claude/prd-places-of-interest.md` v2.8, §8.1 (срез S6′)
- **чей документ:** продакт
- **исход:** меняет проверку
- **статус:** открыт
- **автор:** тестирование, 2026-08-29
- **внесено:** —

В проверяемую ветку влит `dev` с PR #56, который снял неслоёный `* { padding: 0; margin: 0 }` и
тем **оживил 49 утилит отступов в 12 компонентах** — PR перечисляет их сам: AboutTour,
BookingForm, ContactForm, FeedbackForm, Modal, RecentEventCard, RecentEventsSection, ReviewCard,
ReviewsSection, TourCard, TourSelect, ToursSection. Один побочный эффект в том же PR уже
чинился (`mx-auto` на карточке прошедшего тура). В самом `PoiList` ожили три утилиты, и одна из
них — `py-3.5 pr-7` на ссылке-иконке — впервые делает существующей мишень 44 × 44 из дизайна
§3.6 п. 5, то есть до влития это требование не выполнялось вовсе, а после стало выполняться без
единой строки правки в срезе.

§9.4 перечисляет, что обязано остаться нетронутым: `meetingPoint`, `bonus`, число URL, строки
через next-intl, зелёный билд, деление по дням. Ни один из шести пунктов не покрывает изменение
общего CSS всего сайта, а AC 22 («билд проходит») на отступы слеп по определению. Единственная
сеть — AC 23, взгляд на мобильном, и он в этом прогоне провален по другой причине.

Вопрос ровно один и он процедурный: **что считается базой сравнения для среза, в который влит
чужой PR такого класса** — точка ответвления ветки (тогда 12 компонентов входят в объём этого
прогона и в нём же должны быть осмотрены) или `dev` после влития (тогда они уже приняты и
проверяются отдельно). Сам я решить это не могу: расширить объём приёмки фичи на чужой PR —
не моя граница.

**Что нужно от ответа.** Одна строка: база — точка ответвления или влитый `dev`. Прогон вынес
последствия PR #56 в отдельный раздел отчёта и ничего по 11 чужим компонентам не проверял.

**Ответы:** —

---

## T17 — A13: «серверный HTML в обоих случаях один и тот же» не может провалиться

- **документ:** `.claude/architecture-places-of-interest.md` v1.3, §8 A13
- **чей документ:** архитектура
- **исход:** к сведению
- **статус:** открыт — ответ внесён, закрывает владелец
- **автор:** тестирование, 2026-08-29
- **внесено:** `architecture-places-of-interest.md` v1.4 §8 A13, шапка

A13 требует: «с выключенными скриптами … с включёнными … **серверный HTML в обоих случаях один
и тот же**, и в консоли нет предупреждений о гидратации. Последняя половина обязательна: она и
есть проверка запрета о расхождении».

Первая из двух половин не проваливается ни при какой реализации: ответ сервера не зависит от
того, исполняет ли клиент скрипты — сервер об этом не знает и знать не может. Свидетельство
получается всегда и ничего не подтверждает; проверено в этом прогоне curl'ом — байты одинаковые,
но одинаковыми они были бы и при нарушенном запрете.

Запрет §4.3.2 нарушается не расхождением двух серверных ответов, а расхождением **серверного
HTML с первым клиентским рендером**. Ловит его ровно вторая половина, и она в A13 уже названа
обязательной. То есть проверка верна по существу, а её первая половина — лишний шаг, который
проверяющий может принять за доказательство.

На прогон это не влияет: A13 закрывается второй половиной, и она в браузерном заходе чистая.

**Что нужно от ответа.** Ничего не блокирует. Если первая половина задумана как сравнение
серверного HTML с DOM после гидратации — это стоит написать, потому что это другая проверка.

**Ответы:**

- **2026-08-29, архитектура — принято, лишний шаг убран.** Замечание про «к сведению» скромнее,
  чем оно есть: шаг не просто лишний — он выглядит доказательством и им не является. Ответ
  сервера от того, исполняет ли клиент скрипты, не зависит и зависеть не может, значит байты
  совпали бы и при нарушенном запрете; проверяющий, получивший совпадение, вправе счесть запрет
  проверенным.
  Сравнение двух серверных ответов из A13 убрано. Обязательной половиной названы **предупреждения
  о гидратации** — они и ловят то расхождение, которое запрет описывает: серверного HTML с
  **первым клиентским рендером**. Ваша догадка про замысел записана как отдельная, необязательная
  проверка: сравнить серверный ответ с DOM после гидратации — это другое сравнение, и оно
  свидетельствует о другом.
  → внесено: `architecture-places-of-interest.md` v1.4 §8 A13, шапка

---

## T18 — AC 23 выполняется комментарием бота, то есть не различает PR

- **документ:** `.claude/prd-places-of-interest.md` v2.8, §9.4 AC 23; `CLAUDE.md` (раздел
  Deployments); `.claude/context.md` DEFINITION_OF_DONE
- **чей документ:** продакт
- **исход:** меняет проверку
- **статус:** открыт
- **автор:** тестирование, 2026-08-29
- **внесено:** —

AC 23: «The PR **states its Vercel preview URL** and says a mobile check **is** needed».

На PR #52 вторая половина выполнена явно и в теле («A mobile look is needed — most of what this
PR adds only happens after hydration, and the collapsed card is mobile-only»), а первая — нет:
в теле PR ровно одна ссылка, и она на `claude.com/claude-code`; раздел «What to look at on the
preview» адреса не содержит. При этом адрес превью на странице PR **есть** — в автоматическом
комментарии бота Vercel, который приходит на каждый PR без участия автора.

Отсюда два прочтения, дающие противоположные исходы: «на странице PR адрес присутствует» —
выполняется всегда, у любого PR, то есть критерий не различает ничего; «адрес назвал автор
PR» — критерий проваливаем и на этом PR провален.

Прогон вынес «не выполнено» по второму прочтению, потому что первое делает критерий
непроверяемым. Но выбор снова за проверяющим, а не за документом.

**Что нужно от ответа.** Одна строка: считается ли комментарий бота исполнением AC 23. Если да —
критерий стоит убрать, он не работает; если нет — так и написать, «в теле PR».

**Ответы:** —
